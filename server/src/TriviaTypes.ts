export enum GamePhase {
    Lobby = "lobby",
    Question = "question",
    Answer = "answer",
    GameEnd = "gameend"
}

export enum PlayerRole {
    Contestant = "contestant",
    Chaser = "chaser"
}