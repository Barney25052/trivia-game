# Tickets

Granular, agent-sized work items. One ticket = one task = one agent session (or one small PR).

## Rules

- **One ticket per agent**: a ticket must fit in a single working session. If it can't, split it.
- **Don't edit tickets you're not working on** (leave a note if you must).
- Each ticket has verifiable **Acceptance criteria** — run the listed commands to prove it's done.
- **Dependencies** order the queue. A ticket whose dependencies are incomplete is not ready to start.
- When you start/finish a ticket, move its row in the status table below.

## Ticket template

```
# ###: Title
## Goal            — what this buys the project (1-2 lines)
## Scope           — files touched, what to do, what NOT to do
## Acceptance      — commands to run + expected result
## Dependencies    — ticket numbers that must be done first
```

## Status

| # | Title | Status |
|---|-------|--------|
| 001 | Remove dead `GamePhase` copy in `common/` | done |
| 002 | Server game-config constants + role enum | done |
| 003 | Cancellable room timer utility | done |
| 004 | Open-ended question bank: seed data | done |
| 005 | Question bank loader + random picker | done |
| 006 | New asymmetric game state schema | done |
| 007 | Pure game-flow state transition function | done |
| 008 | Rewire room to new state, phases, timers | done |
| 009 | Client: new phase screens + flow | done |
| 010 | Client: make SERVER_URL configurable | done |
| 011 | Stale test/loadtest cleanup + green baseline | done |
| 012 | De-template legacy cruft (`MyRoom` → `TriviaRoom`, dead phases/schema) | done |
| 013 | Server: authoritative chaser selection (random + vote) | done |
| 014 | Client: chaser-selection screens (mode pick + votes) | done |
| 015 | Client: show roles + rules to all players | done |
| 016 | Harden player setup (server-side name validation) | done |
| 017 | Fix flaky `roomFlow` end-to-end test (server) | done |
| 018 | TriviaTypes.ts trailing-newline parity (server vs client) | done |
| 019 | Chaser selection: random-mode animation (client) | done |
| 020 | Lobby screen polish — readable names, settings to the side, drop "How to Play" | done |
| 021 | Server: roles-reveal ready gate + cash-builder cooldown | done |
| 022 | Client: roles-reveal ready vote + get-ready countdown (depends 021) | done |
| 023 | Authority + phase guards on offer/chase/final handlers (server) | done |
| 024 | Clamp room-option duration overrides to gameConfig bounds (server) | done |
| 025 | Make "random" the explicit default chaser-selection mode (kills the silent `""`) | done |
| 026 | Drop the synced GamePlayer.sessionId field — use map keys for identity | done |
| 027 | Remove template cruft and dead CSS | done |
| 028 | Clear the chaser-wheel overlay on leave/disconnect | done |
| 029 | Move maxClients into gameConfig; remove raw join-options log | done |
| 030 | Add per-round question event schema + first-class seat/spectator axis | done |
| 031 | Extract TriviaRoom handler dispatch into a handlers module | done |
| 032 | Normalise GamePhase.GameEnd value to camelCase | done |
| 033 | Move chaser wheel overlay into its own screen in the phase flow | done |
| 034 | Rekey players map with seat ID — stop leaking sessionIds to clients (absorbs 026) | done |
| 035 | Shared type definitions for server→client message payloads | done |
| 036 | Chaser character picker during roles-reveal, identity revealed at offer stage | done |
| 037 | Per-player message rate limiting | done |
| **Phase 2 — Cash Builder** | | |
| 038 | Answer validation utility for open-ended questions | done |
| 039 | Server: question delivery, answer handler, pot tracking | done |
| 040 | Client: cash builder screen — question display, submission, pot | done |
| 041 | Phase 2 integration tests — full cash builder flow | done |
| 042 | Recover the game flow when the active player leaves mid-round | done |
| 043 | Rename `cashBuilderQuestionsAsked` → `cashBuilderCorrectAnswers` (truthful correct count) | done |
| 044 | Finish the seat-id rekey — players map keyed by `GamePlayer.seatId`, not `sessionId` | done |
| 045 | Null-guard `onLeave` before dereferencing `player.seatId` | done (covered by 042) |
| 046 | Remove dead CSS — `.rotate` rule and `pan-bg` keyframe | done |
| 047 | Reconcile the answer checker with ticket 038's spec — then wire `ANSWER_CHECK` | done |
| 048 | Cash builder — pot/score doesn't visibly increase during the round | done* |
| 049 | Contestant lineup intro screen before the first cash builder | done |

\* 048 closed after only proving the server patch path — the on-screen render fix never landed; see ticket 053 (reopened by user report, `bug-005`).

### Phase 3 — The Offer
| 050 | Chaser pot — lifecycle, payouts, and cap (server) | done |
| 051 | Chaser sets the high & low offers; contestant picks (server) | done |
| 052 | Offer screen — Chaser sets high/low with pot visibility; contestant picks; spectators wait | done |
| 055 | Persistent Chaser quip channel (server) | done |
| 056 | Persistent Chaser panel — box, portrait, and quip bubble across every phase (client) | done\*\*\* |
| 057 | Sync the Offer screen's auto-generated quips across clients (`bug-007`) | done |

### Follow-up bug fixes
| 053 | Cash builder — on-screen pot/count still don't increase (`bug-005`) | done\*\* |
| 054 | Random chaser — skip the ChaserSelection player-list hold, go straight to the wheel (`bug-006`) | done |
| 058 | Fix the impossible low offer when a contestant banks $0 and the team pot is $0 | done\*\*\*\* |
| 060 | Fix `roomFlow` test race — `phases` array misses `RolesReveal` broadcast (`bug-003`) | done |
| 061 | Recover the game when the last contestant leaves during the `Lineup` hold (`bug-004`) | done |
| 062 | Fix cash-builder answer input silently swallowing submissions (`bug-008`) | done |
| 068 | Fix `offerQuips` test flake — `waitForMessage("offerStart")` registered after the broadcast (`bug-009`) | done |

### Phase 3 follow-up — new feature
| 059 | Chaser character reveal — new phase after the first cash builder, before the first offer | done |

### Phase 4 — The board chase
| 063 | OpenTDB multiple-choice source behind the get-questions interface | done |
| 064 | Server — authoritative board-chase engine | done |
| 065 | Client — real board-chase screen (replaces the placeholder) | done |
| 066 | Phase 4 integration tests — full board-chase flow | done |
| 069 | Highlight the contestant's and Chaser's *current* board space | done |
| 070 | Lay out chase answer buttons side-by-side instead of stacked | done |
| 071 | Chase question prompt is using the wrong font | done |
| 072 | Chase lockout — strong, shared pulsing cue (background + buttons) synced from a `chaseLockoutStarted` broadcast | done |
| 073 | Chase correct/wrong answer reveal never renders (raced out by the next question) | done |

### Phase 4 review follow-ups (verified in the Phase 4 review — 211 tests green, both builds green)
| 074 | Chase stalls permanently when the MC question source fails or returns nothing (bounded retry → local MC backup pool → resolve as caught only if the backup runs out) | done |
| 075 | Chaser disconnect mid-game resolves the room to GameEnd, team wins (`bug-010`) | done |
| 076 | Chase authority-guard test gap — non-participant cannot answer; active-contestant leave mid-Chase | done |
| 090 | Local ~100-question MC backup question bank (`server/data/mc-backup.json`, OpenTDB `type=multiple` shape) loaded through the get-questions interface as the chase's fallback source | done |

### Phase 5 — The final round
| 077 | Server — final-round question delivery for the team and Chaser streams (per-side, non-repeating) | done |
| 078 | Server — team answers: buzz-in gate + `submitFinalAnswer`, `teamScore + 1` | done |
| 079 | Server — Chaser answer engine: `chaserScore`, win on reach (tie counts), steal/push-back (floor at 0 → raise `teamScore`), remove placeholder `finalChaserScore` | done |
| 080 | Client — real Team Final screen (UI sign-off required) | done |
| 081 | Client — real Chaser Final screen incl. steal prompt (UI sign-off required) | done |
| 082 | Phase 5 integration tests — full final round e2e on the server | done |
| 083 | Game-end results screen from real scores + drop dead `GamePlayer.score` (UI sign-off required) | done****** |

### User-reported polish & bugs (084–089, from 2026-09-12 review)
These pulled the Chase/CashBuilder/palette items out of the 067 whole-app pass early by direct user direction.
| 084 | Chaser character reveal — name text flashes black/white-outline before white (`bug-011`) | done |
| 085 | Chase screen redesign — 16:9 table layout: Chaser left, board centre, contestant profile right (visible all round), question + horizontal answer buttons bottom (supersedes the 070 wrap) | done |
| 086 | Cash builder — active contestant's profile on the right + answers pop out of a speech bubble, visible to all | done |
| 087 | Chase lockout pulse — full-screen dark-blue background instead of a black question-area flash | done |
| 088 | Colour system — tokenise every colour in `style.css` into `:root` CSS custom properties; brighten the contestant blue | done |
| 089 | Bug — correct MC answer not highlighted on the chase reveal (`bug-012`) | done |

### Question-bank editor — web UI (user-reported 2026-09-12)
The first in-game path to adding open-ended questions (the editor stretch goal); other editor ideas stay backlog.
| 091 | Server — `POST /api/questions` endpoint: validate + atomically append to `server/data/questions.json` (no `category`, legacy) | done\*\*\*\*\* |
| 092 | Client — home-page "Add questions" entry + Add Question screen (question / answer / alternatives) → posts to 091 (UI sign-off required) | done******* |

### Final-round feedback polish (user-reported 2026-09-12)
The "it just hangs" steal interaction plus the final-round choreography the user pulled forward from the 067 pass.
| 093 | Client — Team Final target/steps row, parity with the Chaser Final's (shared row, box per `teamScore` point; starts at survivors, +1 per right answer) | done******** |
| 094 | Server — pause the Chaser's final clock during a steal window (resumable countdown) | done |
| 095 | Server — steal transport: broadcast the winning steal answer (`seatId`, text), send `finalSteal` to the Chaser too, reveal outcome to both sides, hold `stealResolveHoldMs` (~3s) before the next chaser question | done |
| 096 | Client — steal-table interaction on the Chaser Final: answer pops from the submitter's seat on all clients, outcome + 3s countdown, and the Chaser sees the team table during a pushback (depends 095; UI sign-off required) | done********** |
| 097 | Client — no quip/message box for the Chaser on the Chaser Final (panel + bubble stay) | done |
| 098 | Client — Chaser's final-chase answers pop from the Chaser's speech bubble (their own client only) | done********* |
| 099 | Client — final-round answer bubbles hold ~3s instead of clearing the instant the next question arrives | done*********** |
| 100 | Client — green background flash when a final-round answer is right (correct buzzer answer + correct steal) | done************ |

### Custom contestant characters (drafted 2026-09-12)
Custom-pickable contestant avatars (5 hairstyles, 3 faces, hair/face/shirt colour × 9); contestants only — the Chaser keeps the roster portraits. Codec `[hairStyle][hairColour][faceStyle][faceColour][shirtColour]`. Colours = grayscale art tinted via CSS. Reactions: neutral default, correct → smile, wrong → frown, two wrong in a row → teary; flashes ~3s then neutral.
| 101 | Server — character codec + `GamePlayer.character` + validated `setCharacter` | done |
| 102 | Client — lobby character picker + shared `CharacterFace` component + greyscale-tint palette (UI sign-off required) | done************* |
| 103 | Contestant reactions — server `reaction` cues + flash/revert face expressions (UI sign-off required) | done************** |

### Whole-app polish
| 067 | Full layout/design pass across all screens once every phase is implemented (discussion, not a solo build) | backlog |

### Follow-up bug fixes (found 2026-09-13, overnight session)
| 104 | Fix invalid WebSocket close code on host disconnect (`bug-014`) | done*************** |

### User-reported polish & bugs (105-111, live session 2026-09-13)
| 105 | Chaser Final round doesn't end at 0; clock must genuinely pause during a pushback (`bug-013` + user report) | done**************** |
| 106 | Stale Cash Builder question briefly visible after the phase moves on | done***************** |
| 107 | Cash Builder speech bubble doesn't stay up long enough | done****************** |
| 108 | Character colours render too lightly; head sits too high (UI sign-off required) | done******************* |
| 109 | Character layer images flash/look broken on first load | done******************** |
| 110 | Contestants' custom characters aren't shown on the Roles Reveal screen (UI sign-off required) | done********************* |
| 111 | Layout stability — stop elements shifting/jumping across screens (UI sign-off required) | done********************** |
| 112 | Cash Builder's "Time left" display is hardcoded, disconnected from the real duration (`bug-015`) | done*********************** |
| 113 | Cash Builder's correct-answer screen flash can stay stuck on past its 700ms timer (`bug-016`) | done************************ |
| 114 | `chaseFlow` lockout-window test is timing-flaky (`bug-017`) | done************************* |

### Visual redesign — "Big Baws Style Guide" (agreed via mockup with the user, 2026-09-13, see ticket 067's outcome note)
| 115 | Visual design system foundation (type, colour, base components) | done************************** |
| 116 | Chase screen (the board) visual redesign | done*************************** |
| 117 | Chaser Final + Steal visual redesign | done**************************** |
| 118 | Team Final visual redesign | done***************************** |
| 119 | Lobby visual redesign | done****************************** |

### User-reported polish & bugs, round 2 (live session 2026-09-13, after 105-114 shipped)
| 120 | Lineup screen shows redundant "1st" + "Up first" on the same card | done******************************* |
| 121 | Cash Builder doesn't show the correct-answer text when the contestant gets it right | done******************************** |
| 122 | No transition into the Team Final — the last Chase drops straight into it | done********************************* |

### Autonomous polish pass, round 3 (session starting 2026-09-14, after 115-122 shipped)
| 123 | ChaserSelection (vote) + ChaserReveal (wheel) visual redesign | backlog |

\*\*\* 056: scope was narrowed with the user before implementation (per AGENTS.md's UI sign-off rule) from "always there, every phase" to **only the phases where the Chaser and a contestant are face-to-face**: Offer, Chase, and the Chaser Final — not Lobby, Chaser Selection/Reveal, Roles Reveal, Lineup, Cash Builder, or Team Final. Placement is inline on the left side of each of those three screens (matching where the box already sat in Offer, per ticket 052) rather than a global fixed-position overlay, so `ChaserPanel.vue` is instantiated once per "table" screen instead of a single App-level mount; quip display state (`chaserQuipText`/`chaserQuipKey`) is still centralized in `App.vue` and passed down, with each screen able to feed it local auto-quips (`@auto-quip`) alongside the real `chaserQuip` broadcast from ticket 055. `OfferScreen.vue`'s inline chaser box/portrait/bubble markup was extracted into the shared component as scoped. Verified manually with two browser clients through a full round (Lobby → chaser pick → Offer, incl. a live Chaser-typed quip arriving on the other client → Chase → Team Final (panel correctly absent) → Chaser Final); found and logged an unrelated pre-existing bug in the process (`bug-008`, `CashBuilderScreen.vue` answer input).

\*\*\*\* 058: found already implemented in code (server + client) but left marked `backlog` in this table — corrected 2026-09-12, no new work needed.

\*\*\*\*\* 091: `POST /api/questions` — pure `validateNewQuestion`/`appendQuestion` in `server/src/questions/bankAdmin.ts` (`BANK_EDIT` caps in `gameConfig.ts`, typed errors, no `category` on appended rows), exported `QUESTIONS_PATH` from `bank.ts` so the endpoint reads+writes the same file, and the route in `server/src/app.config.ts` (dev CORS + `express.json`, atomic write via tmp file + rename that preserves the bank's CRLF line endings so an append doesn't reformat the 572-row file — `201 { id, question, answer, alternatives }` / `400 { error }`). An added question applies to the **next** room; refreshing in-flight rooms is out of scope. `server/test/bankAdmin.test.ts` covers the pure module + the endpoint against the shared `testServer.ts` boot, snapshot/restoring `questions.json` around each HTTP test so the seed data stays byte-identical.

\*\* 053: root cause was in `client/src/App.vue` — `activeContestantMoney`/`activeContestantCorrectAnswers` were computed from `activeContestant.value` (itself a computed). The active contestant's Colyseus schema instance keeps the same object identity across state patches (only its properties mutate), so Vue's computed never saw the *reference* change and never re-fired the downstream computeds, even though `players.value` (a plain ref, reassigned to a fresh array on every patch) was updating correctly. Fixed by having each derived computed read `players.value.find(...)` directly instead of chaining through `activeContestant`. Also added the wrong-answer flash/reveal from the ticket's scope: server now sends a per-client `answerResult` message (`correct`, `correctAnswer`) on every `submitAnswer`, and holds the next question for `wrongAnswerRevealMs` (2.5s default, room-configurable, see `CASH_BUILDER.wrongAnswerRevealMs` in `gameConfig.ts`) on a wrong answer so the contestant can read the correct answer while the screen flashes red; a correct answer flashes green immediately. No Vue test harness exists, so this was verified manually via two browser SDK clients (see `server/test/cashBuilderFlow.test.ts` for the automated server-side coverage of `answerResult` and the reveal delay) — to re-verify by hand: start `server` + `client` dev, join two browsers, answer correctly (pot/count should update on both the active player's and the spectator's screen instantly) and answer wrong (screen should flash red and show "Correct answer: …" for ~2.5s before the next question, with input disabled during that window).

\*\*\*\*\*\* 083: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop). Removed the dead `@type("number") score` from `GamePlayer` (`server/src/rooms/schema/GameState.ts`) and its one remaining reference (`server/test/gameState.test.ts`'s default-value assertion) — grep confirmed no other reader in `server/src` or `client/src`. Rewrote `ResultsScreen.vue` to render the winner banner (`endGame.winner`), `teamScore` vs `chaserScore`, the banked `teamPot`, and a survivor count computed as `players.filter(p => p.role === Contestant && p.madeItBack).length` — matching the same computation `startFinalTeam` uses server-side to seed `teamScore`; per-contestant rows show `cashBuilderMoney` and a made-it-back/caught tag, never the removed `score`. `App.vue` already held `teamScore`/`chaserScore`/`teamPot` refs from earlier tickets, so no new state needed lifting — just threaded through as props. Judgment calls not spelled out in the ticket: (1) exact winner-banner copy taken verbatim from the ticket's own proposed direction ("The Chaser caught you" / "The team made it back"); (2) new CSS classes (`resultsBanner` + `-teamWin`/`-chaserWin` modifiers, `resultsPlayerList`, `resultsPlayerRow`, `resultsStatus-back`/`-caught`) follow `style.css`'s actual established convention — camelCase base names with a hyphenated modifier suffix (`chaseOutcomeBanner-escaped`, `chaserFinalTargetBox-filled`, etc.) and consistently 2-space indentation — rather than AGENTS.md's literal "4-space indent, kebab-case" wording, since the file itself is 2-space/camelCase throughout and internal consistency wins; (3) the winner banner reuses `chaseOutcomeBanner`'s color gradients but as a normal in-flow block instead of its `position: fixed` overlay, since here it's the screen's own primary content, not something laid over another screen; (4) reused `teamFinalPotBox`/`teamFinalPotAmount` verbatim for the pot display and the `"$" + n.toLocaleString("en-US")` currency format, matching `TeamFinalScreen.vue`. Found and fixed one pre-existing bug in scope: a global `ul { background-color: var(--color-white); ... }` rule (`style.css`, originally for the vote/lobby card lists) silently applied to the results player list too, painting a white box with invisible white-on-white text — the original `ResultsScreen.vue` had the same `<ul>/<li>` structure and would have hit the identical bug had it ever rendered real content. Fixed by using plain `<div>`s instead of `<ul>/<li>` for that list. `server/test`, `server build`, and `client build` all green (291 tests). Live-verified in two real browser tabs against the dev servers (temporary, reverted-before-commit room-option overrides shortened the server-authoritative round durations for the smoke test only — no production code path was changed): a full game Lobby → chaser pick → cash builder → offer → chase escape → Team Final → Chaser Final timeout reached `GameEnd` with correct data ("The team made it back", Team 1 — Chaser 0, 1 of 1 made it back, $8,000 pot, Bob's row correctly showing his real $0 `cashBuilderMoney` distinct from the $8,000 pot); a second quick game forced the chaser-win path (last contestant left mid-Lineup) and rendered "The Chaser caught you" with 0/0/$0 and no player rows, without crashing on the empty contestant list. No console errors on either client in either run.

\*\*\*\*\*\*\* 092: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as ticket 083's `******` footnote. New `client/src/screens/AddQuestionScreen.vue` (question input, answer input, a multiline "alternative answers" textarea split on newlines client-side into an array, Submit/Back), routed locally in `App.vue` exactly like `home` — a new `addQuestionMode` ref that `currentScreen` checks when there's no room, with `showAddQuestion`/`hideAddQuestion` toggling it — since this screen never touches a Colyseus room. `HomeScreen.vue` gained a quiet "Add questions" text-link under the join/create buttons (`add-questions` emit, kebab-case matching this codebase's other multi-word custom events like `submit-answer`/`send-quip`, unlike the plain `join`/`create` single-word ones already there). Judgment calls not spelled out in the ticket: (1) the screen derives its own http base from `SERVER_URL` independently (`import.meta.env.VITE_SERVER_URL ?? "ws://localhost:2567"`, same default as `App.vue:20`) rather than taking it as a prop, since the screen is otherwise fully decoupled from `App.vue`'s room/session state — and the derivation was written to correctly handle `wss://` → `https://` too (a plain `/^ws/` replace would mangle `wss://` into `httpss://`), not just the `ws://` → `http://` case the ticket called out; (2) new CSS classes (`addQuestionsLink`, `addQuestionTextarea`, `addQuestionError`, `addQuestionSuccess`) follow `style.css`'s actual established convention — camelCase, 2-space indent — rather than AGENTS.md's literal "4-space indent, kebab-case" wording, consistent with the precedent already noted in ticket 083's footnote; `addQuestionError`/`addQuestionSuccess` intentionally aren't a shared/generic class since the one existing error-text class (`offerErrorText`) is only ever used within `OfferScreen.vue` — each screen owns its own similarly-styled variant; (3) client sends whatever is typed straight to the server with no front-end non-empty validation, so an empty submit round-trips to 091's real 400 and its exact message renders inline, per the ticket's acceptance criterion; (4) `maxlength` attributes on the question/answer inputs mirror `server/src/gameConfig.ts`'s `BANK_EDIT` caps (duplicated client-side as plain constants, the same no-shared-module pattern `OfferScreen.vue` already uses for `OFFER`/`BOARD`) as a UX nicety only — the server remains the sole source of truth and re-validates on submit. `client build` and `server test`/`build` all green (291 server tests unaffected). Live-verified against both dev servers: home shows the quiet link; Add Question screen renders; submitting empty shows the server's real `"question must be a non-empty string of at most 300 characters"` inline in red; submitting a full valid question (with two newline-separated alternatives) showed "Added question #816." and the row landed in `server/data/questions.json` with the correct next id and no `category` field; "Add another" resets the form; "Back" returns Home; Create Lobby was then exercised end-to-end (room created, seated as host) to confirm the new routing didn't disturb the existing room flow. The test row was removed from `questions.json` before committing (`git checkout` back to the committed version) so the bank stays clean. No `v-html` anywhere — grepped to confirm. No bugs found in scope; nothing added to `BUGS.md` for this ticket.

\******** 093: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092's footnotes. Extracted the Chaser Final's existing target row into a shared pair of classes — `chaserFinalTargetRow`/`chaserFinalTargetBox`/`chaserFinalTargetBox-filled` renamed to `finalTargetRow`/`finalTargetBox`/`finalTargetBox-filled` in `style.css` and `ChaserFinalScreen.vue` — and rendered the same row in `TeamFinalScreen.vue`, placed directly under the header/score line and above the question area, with an identical `targetBoxes` computed (`1..teamScore`, filled when `box.index <= chaserScore`). Judgment calls not spelled out in the ticket: (1) `TeamFinalScreen` didn't previously receive a `chaserScore` prop — added `:chaserScore="chaserScore"` to its `<TeamFinalScreen>` invocation in `App.vue` alongside the existing `:teamScore`, since `chaserScore` was already a top-level synced ref there (used by `ChaserFinalScreen`/`ResultsScreen`) and the ticket explicitly says the server needs no changes because both scores are "already synced to all clients"; (2) kept the local `targetBoxes` computed name as-is in both screens (only the CSS/template class strings were the "cruft" the ticket called out — the JS-side computed was already generically named and duplicating its four-line body in the sibling screen was simpler and more readable than inventing a shared composable for one ticket's scope). `client build` (`vue-tsc -b && vite build`) and `server test`/`build` (291 tests) all green; repo-wide grep confirms zero remaining references to the old `chaserFinalTargetRow`/`chaserFinalTargetBox` names outside this ticket's own file and this README's history. Live-verified with two real browser clients through a full round (Lobby → random chaser pick → cash builder → high offer → chase escape → Team Final → Chaser Final): Team Final opened with exactly 1 box (matching the 1 survivor), a correct buzzed-in answer bumped `teamScore` to 2 and grew the row to 2 boxes on **both** clients simultaneously; the Chaser Final then rendered the identical row (`Chaser 0 — Target 2`, 2 unfilled boxes) and a correct Chaser answer filled box 1 (red glow) on both clients while box 2 stayed unfilled. No bugs found in scope; nothing added to `BUGS.md` for this ticket.

********* 098: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092/093's footnotes. Gave `ChaserPanel.vue` a dedicated second bubble driven by new `answerText`/`answerKey` props, kept entirely separate from the existing `quipText`/`quipKey` props/broadcast channel — the ticket left the prop/slot design up to this session, and a dedicated pair was chosen over overloading the quip bubble so a future broadcast quip (099/100 territory) can never clobber, or be clobbered by, the Chaser's own answer bubble; the new bubble `<div>` reuses the `chaserPanelBubble` class for the shared speech-bubble look and adds one small modifier, `chaserPanelAnswerBubble` (`style.css`, `margin-bottom: 60px`), so the two bubbles stack instead of exactly overlapping in the case both are visible at once (the Chaser Final still fires a `START_QUIPS` auto-quip on mount, and 097 only hid the quip *input*, not the display channel). `ChaserFinalScreen.vue` owns the new local state (`chaserAnswerBubbleText`/`chaserAnswerBubbleKey`), set synchronously in `submitChaserAnswer()` right after emitting `submit-final-chaser-answer` — mirroring `TeamFinalScreen.vue`'s existing `bubbleText`/`bubbleKey` pattern exactly — and cleared by a plain local `setTimeout` (`ANSWER_BUBBLE_HOLD_MS = 3000`) or immediately when the next `finalQuestion` prop arrives, whichever comes first; kept intentionally simple and localized (no shared composable) per the ticket's own note that ticket 099 will later generalize a min-lifetime pattern across the final-round bubbles. Confirmed no server changes were needed as the ticket predicted: `submitFinalChaserAnswer` (`server/src/rooms/handlers/messageHandlers.ts`) already sends `answerResult` only to the submitting client via `client.send(...)` (never a room broadcast) and that payload never carries the raw typed answer (only `correct`/`correctAnswer`/`questionId`) — so the team was already structurally incapable of seeing the Chaser's text before this ticket, and this ticket only had to make the Chaser's *own* client render it. `client build` (`vue-tsc -b && vite build`) and `server test`/`build` (291 tests) both green. Live-verified with two real browser clients (temporary, reverted-before-commit room-option overrides shortened round durations for the smoke test only, same approach as tickets 083/093) through Lobby → chase escape → Team Final → Chaser Final: a correct Chaser submission rendered the typed text live in a `chaserPanelBubble chaserPanelAnswerBubble` DOM node on the Chaser's own client at the exact moment of submission (captured via a DOM query chained immediately after the keypress, since the client advances to the next final question fast enough that a manual screenshot round-trip alone kept missing the transient bubble), and the team client's screen showed only the generic "The Chaser is answering…" message throughout, with no trace of the typed text at any point in either the DOM or on screen. No bugs found in scope; nothing added to `BUGS.md` for this ticket.

********** 096: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092/093/098's footnotes. `ChaserFinalScreen.vue`'s bottom area now checks `stealActive` before `isChaser`, so a live steal renders the `chaserFinalTeamRow` table to every seat including the Chaser's own client, replacing the old static "Wrong! Waiting to see if the team steals…" message (that message is kept only as a brief fallback for the instant between the Chaser's own wrong `answerResult` and the room-wide `finalSteal` broadcast landing, since both fire from the same server handler call over the same ordered connection). Added a `finalStealAnswer` prop (new `App.vue` ref + `onMessage` handler, threaded through alongside the already-wired `finalSteal`/`finalStealResolved`) and rendered it as a `chaserPanelBubble`-style bubble keyed by `p.seatId === stealAnswerSeatId`, placed as a direct child of each `.teamFinalPlayer` cell exactly like `TeamFinalScreen.vue`'s own-seat bubble — except here it matches the *broadcast* submitter's seat, not `mySeatId`, so it pops in the same place on every client (Chaser included). Fixed a latent bug in the pre-existing `finalStealResolved` watcher while wiring the outcome line: it unconditionally rendered "Stolen!"/"target goes up" text regardless of `result.correct`, which was correct back when only a *correct* steal ever reached that watcher, but 095 extended `finalStealResolved` to broadcast on the *wrong* case too — so the outcome line now branches on `result.correct` first, and the redundant `answerResult`-driven wrong-branch (which raced the same ref from a different message, per the now-stale comment above it) was removed outright rather than left to race against the new logic. Added a second, independent hold-countdown ticker (`holdEndsAt`/`holdNowTick`, a local `STEAL_RESOLVE_HOLD_MS = 3000` mirroring `gameConfig.ts`'s `FINAL_STEAL_RESOLVE_HOLD_MS` the same way `CHASER_FINAL_SECONDS` already duplicates `chaserDurationMs`) that starts on `finalStealResolved` *and* on the pre-existing local unclaimed-expiry timer, rendering a plain 3→2→1 `{{ holdSecondsLeft }}` count with no outcome line for the unclaimed case; it shares the existing `closeTimeout`/`manuallyClosed` mechanism to reset the question area once the hold elapses. Flash: reused the existing `chaseLockoutFlash` layer for the "steal is open" pulse and, once `finalStealResolved` sets `stealOutcomeCorrect`, swap it for a new `chaserFinalStealFlash`/`-correct`/`-wrong` pair — same fixed full-viewport + `transition: background-color` pattern as `.cashBuilderScreenFlash`, tinted with the identical `--color-green-flash-rgb`/`--color-danger-strong-rgb` variables the cash builder's flash already uses — left simple per the ticket's own note for 100 to extend. Judgment calls not spelled out in the ticket: (1) `finalStealAnswer`'s prop shape is the raw server message with no synthetic key field — Vue's `watch` already fires on the new object reference every broadcast, so a per-seat local `stealAnswerBubbleKey` counter (bumped in the watcher, mirroring `chaserAnswerBubbleKey`'s existing pattern) drives the `Transition` instead of asking `App.vue` to manufacture one; (2) new CSS classes (`chaserFinalStealBubble`, `chaserFinalStealFlash(-correct/-wrong)`, `chaserFinalStealOutcome(-correct/-wrong)`, `chaserFinalStealCountdown`) follow `style.css`'s actual established convention — camelCase, 2-space indent, `"Roboto", sans-serif` — rather than AGENTS.md's literal "4-space/kebab-case" wording or its stale "Luckiest Guy" font claim, consistent with the precedent already noted in tickets 083/092/093's footnotes (confirmed by grep: the file uses "Roboto" throughout and "Luckiest Guy" appears nowhere in it); (3) the Chaser's own `secondsLeft`/`CHASER_FINAL_SECONDS` display ticker was left untouched (out of scope — see `bug-013` below). `client build` (`vue-tsc -b && vite build`) and `server test`/`build` (291 tests, no server files touched) both green. Live-verified with three real browser clients (temporary, reverted-before-commit `server/src/gameConfig.ts` overrides — shrunk `BOARD` to a 1-space chase and shortened `CASH_BUILDER`/`CHASER_REVEAL`/`CHASER_CHARACTER_REVEAL`/`TEAM_FINAL`/`REVEAL_READY`/`LINEUP`/`FINAL_STEAL_WINDOW` durations — sped the smoke test only; `git checkout -- server/src/gameConfig.ts` restored the committed file before committing, confirmed by `git status`/`git diff --stat`) through Lobby → random chaser pick → two contestants' cash builder/offer/chase → Team Final → Chaser Final: forcing a Chaser miss showed the team table plus "Steal! Xs left" identically on all three clients, including the Chaser's own (no more static waiting message); a teammate's wrong steal answer popped a bubble with the typed text over exactly that seat on every client and showed "Wrong — the answer was …" in red with a red full-screen flash and a 3→2→1 countdown; a second, correct steal (Chaser already at 0) popped the bubble over the correct — different — seat on a third client that was neither the submitter nor the Chaser, with "Correct! The Chaser was already at zero — the target goes up." in green, a green flash, the target row growing by one box, and the same countdown; both times the flow correctly resumed to a fresh Chaser question afterward. Found and logged one pre-existing, out-of-scope bug while testing: `bug-013` (`ChaserFinalScreen.vue`'s `secondsLeft` display never pauses during a steal window/hold the way the real server clock does per 094/095) — logged to `BUGS.md`, not fixed, per AGENTS.md's bug-triage rule.

\*********** 099: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092/093/098/096's footnotes. `TeamFinalScreen.vue`'s own-seat `bubbleText` now gets a dedicated minimum-life `setTimeout` (`ANSWER_BUBBLE_HOLD_MS = 3000`, same name/value 098 already introduced in `ChaserFinalScreen.vue` for the Chaser's own bubble) instead of being cleared from the `finalQuestion` watch — that watch now only resets `revealedCorrectAnswer`/`answerInput`, leaving `bubbleText` to clear itself on its own timer regardless of how fast the next question arrives. `submit()` clears any pending timeout before arming a fresh one (`clearBubble()`, mirroring `ChaserFinalScreen.vue`'s existing `answerBubbleTimeout` pattern from 098) so a same-seat resubmit re-keys cleanly with no overlapping/leaked timers; `bubbleKey` still bumps every submission so the `chaser-bubble-pop` `Transition` replays. Applied the same treatment to `ChaserFinalScreen.vue`'s steal-table bubble (096's `finalStealAnswer`-driven `stealAnswerText`/`stealAnswerSeatId`): it previously relied entirely on the surrounding steal/hold state machine (`stealActive`/`manuallyClosed`) to keep the bubble on screen, which happens to work today only because `finalStealAnswer` and `finalStealResolved` fire back-to-back from the same server handler call — an incidental coupling, not a guarantee — so it now gets its own `stealAnswerBubbleTimeout`/`clearStealAnswerBubble()` pair, reusing the same module-level `ANSWER_BUBBLE_HOLD_MS` constant rather than a second one, and kept as an entirely separate timer/variable from `startHoldCountdown()`'s `holdTicker`/`closeTimeout` (which still solely govern the outcome banner and when the next Chaser question is allowed in) so the two can never fight over the same state — this was the specific risk flagged going into this ticket, since 096 landed its hold-countdown ticker on this exact screen just before. `style.css` was left untouched: the existing `chaser-bubble-pop` pop-in transition already reads fine held for longer, so per the ticket's own conditional there was nothing to change. `client build` (`vue-tsc -b && vite build`) and `server test`/`build` (291 tests, no server files touched) both green. Live-verified with two real browser clients (temporary, reverted-before-commit `client/src/App.vue` room-create-option overrides — shortened `cashBuilderDurationMs`/`chaserRevealDurationMs`/`chaserCharacterRevealDurationMs`/`lineupDurationMs`/`revealReadyCooldownMs` only, same reverted-before-commit approach as tickets 083/092/093/098 — sped the smoke test only, confirmed by `git diff --stat` showing only the two screen files changed before commit) through Lobby → chase escape (picked the Middle offer so escape needed 5 correct answers, with the Chaser deliberately answering wrong throughout so the round would resolve on the contestant's terms) → Team Final → Chaser Final: a correct buzzed-in answer left the "Spinach" bubble visibly readable over the submitter's seat while the next question had already arrived and a fresh "Buzz in!" button was showing underneath it — the exact fast-chain scenario the ticket describes — and the bubble cleared on its own a few seconds later without ever getting stuck; a second buzz-answer cycle re-keyed the bubble cleanly with no stacking. On the Chaser Final, forcing two separate steal windows showed the steal-table bubble ("Spider", correct) hold through the outcome banner and 3s countdown then clear cleanly when the next Chaser question arrived, and a second steal ("Mathematics", wrong) re-keyed the bubble over the same seat with no leftover state from the first. No new bugs found in scope; `bug-013` (`ChaserFinalScreen.vue`'s `secondsLeft` not pausing during a steal window, logged in 096) is pre-existing and unrelated to this ticket's bubble-timing change, so it was left as-is rather than re-logged.

************ 100: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092/093/098/096/099's footnotes. Consolidated the fixed full-viewport flash layer instead of adding a fourth copy of it: `.cashBuilderScreenFlash`, `.chaserFinalStealFlash` (096), and a new `.finalScreenFlash` now share one grouped-selector rule in `style.css` for the position/fade properties and one each for the `-correct`/`-wrong` green/red tokens, rather than three near-identical blocks — each screen keeps its own class name in its own template (no behavior change to the cash builder or to 096's already-tested steal flash), so this is a pure CSS dedupe per AGENTS.md's no-cruft rule. `TeamFinalScreen.vue` gained the actual new trigger: a `screenFlash` ref + `screenFlashTimeout`, set from the existing `answerResult` watch — correct clears itself after 700ms (`finalScreenFlash-correct`), wrong sets `finalScreenFlash-wrong` and holds until the next `finalQuestion` arrives, mirroring `CashBuilderScreen.vue`'s `screenFlash` timing exactly. Also fixed a real (if latent) bug found while wiring this up: `.teamFinalRoot` was missing the `isolation: isolate` that `.cashBuilderRoot`/`.chaserFinalRoot` already carry for this exact reason — without it the new flash's `z-index: -1` paints behind the whole page instead of just behind this screen's own content, per the stacking-context note already on `.cashBuilderScreenFlash`. `ChaserFinalScreen.vue` needed no behavior change (096's steal flash already covers the correct-steal trigger); only its explanatory comment was updated to point at the new shared rule. Judgment calls not spelled out in the ticket: (1) "who sees it" — went with the ticket's own stated default, the answering player's own client only, and confirmed (not just assumed) this is the only option in scope: every `answerResult` send in `server/src/rooms/handlers/messageHandlers.ts` (cash builder, team final, chaser final, and steal) uses `client.send` to the submitter alone, never a room broadcast, so a "flash every team client" version would need a new server broadcast, which is explicitly out of this ticket's scope; (2) deliberately did **not** add a correct/wrong flash for the Chaser's own initial question in the Chaser Final (as opposed to the steal) — the ticket's own trigger list and acceptance criteria name only the Team Final buzzer and the steal, and semantically a correct Chaser answer is bad news for the team, so reusing "green = good" there would send the wrong signal; left as an explicit non-goal rather than scope creep; (3) added the full-screen red flash to `TeamFinalScreen.vue`'s wrong case in addition to its pre-existing panel-level "✗ WRONG!" reveal (the acceptance criteria allow either alone) to keep the correct/wrong treatment visually symmetric and match the cash builder's precedent literally. `client build` (`vue-tsc -b && vite build`) and `server test`/`build` (291 tests, no server files touched) both green. Live-verified with two real browser clients (temporary, reverted-before-commit `client/src/App.vue` room-create-option overrides — shortened `cashBuilderDurationMs`/`chaserRevealDurationMs`/`chaserCharacterRevealDurationMs`/`revealReadyCooldownMs`/`lineupDurationMs`/`chaserSelectionDurationMs` only, same reverted-before-commit approach as tickets 083/092/093/098/099, confirmed by `git diff` showing zero net change to `App.vue`) through Lobby → chase escape → Team Final → Chaser Final: a correct buzzed-in Team Final answer produced a solid green full-screen flash (screenshotted mid-flash, team score ticking up) that faded after ~700ms; a wrong buzzed-in answer produced the full-screen red flash alongside the pre-existing red reveal panel, confirmed both visually (the expected brick/maroon composite of the 50%-alpha danger-red token over the page's dark body) and via `getComputedStyle` returning exactly `rgba(231, 76, 60, 0.5)` — the intended token value; a correct Chaser Final steal still produced 096's green flash exactly as before, with no double-flash or visual conflict from the CSS consolidation. No new bugs found in scope; `bug-013` (pre-existing, logged in 096, `ChaserFinalScreen.vue`'s `secondsLeft` not pausing during a steal window) is unrelated and was left as-is.

************* 102: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092/093/098/096/099/100's footnotes. Landed the full scope, not a subset: `client/src/character.ts` (already present from 101) is the codec; added a sibling `client/src/characterColours.ts` — kept separate from `character.ts` rather than folded in, so `character.ts` stays a literal, easily-diffable duplicate of the server file per its own "keep both copies identical" header comment — holding the 9-entry colour palette's colour→CSS-filter recipes (`grayscale(1) sepia(1) hue-rotate(...) saturate(...) brightness(...)`, the standard trick for tinting a desaturated image via filter, since `hue-rotate` alone has no effect on a chroma-less pixel) plus matching `--character-colour-0..8` tokens added to `style.css`'s `:root` (ticket-088 token system). New `CharacterFace.vue` (`client/src/components/`) decodes a `character` prop and layers shoulders → face → hair → eyes → mouth using the exact `offerFaceWrap`/`offerShoulders`/`offerFaceLayer` classes the old per-screen markup already used, so no new CSS was needed for the layers themselves; it accepts a `reaction` prop (for 103) but always renders neutral, as instructed. Judgment calls, as flagged going in: (1) **shared 9-colour palette across hair/face/shirt** — went with the ticket's own flagged assumption (one palette, all three channels index into it) since there was no one to confirm with overnight; documented in `characterColours.ts`'s header as a revisit-if-needed call. (2) **placeholder art stand-in** — no real greyscale art exists yet (`HUMAN_TASKS.md` had only `shoulders.png` as `todo`), so `CharacterFace` tints today's existing *colour* placeholder PNGs (`hair-1..5.png`, `face-1..3.png`, `eyes-1/2.png`, `mouth.png`) with the same filter pipeline real greyscale art will get — the tinting visibly muddies/pales the already-coloured placeholders (confirmed live, see below) rather than producing clean hues, which is expected and cosmetic-only; the wiring is correct and ready for real greyscale art to drop in. Added 12 new `HUMAN_TASKS.md` rows (5 hairstyles, 3 faces, 2 neutral eyes, happy/sad/teary eyes — `eyes-teary.png` doesn't even exist as a placeholder yet, mouth neutral/happy/sad/smirk) documenting which layers are greyscale+tinted (hair/face/shoulders, per the codec's 3 colour channels) versus flat/untinted (eyes/mouth have no colour channel) so a human artist isn't told to grey out assets that were never going to be filtered; reconciled rather than duplicated the pre-existing `shoulders.png` row. (3) **`hair-6.png` deletion** — confirmed via repo-wide grep it was referenced only by the five screens' old inline markup being removed in this same ticket and nowhere else, then deleted per the no-cruft rule (the codec only ever supported 5 hairstyles) rather than left as an orphan; not added to `HUMAN_TASKS.md` since it was never a real human-made asset with a documented purpose. Swapped all 6 listed places — `LobbyScreen.vue` (both the new picker's live preview and the seat-circle busts, which previously showed a plain SVG silhouette/initial-letter glyph with no character rendering at all), `ContestantLineupScreen.vue`, `CashBuilderScreen.vue`, `OfferScreen.vue`, `ChaseScreen.vue`, `TeamFinalScreen.vue`, `ChaserFinalScreen.vue` — removing every screen's duplicated face/hair/eyes/mouth image imports and `seatSeed` hash function, and updated `assetPreload.js`'s import list to match exactly what `CharacterFace.vue` uses today (dropped `hair-6.png` and the not-yet-wired happy/sad reaction art, to be re-added alongside 103's reaction wiring rather than preloaded unused now). `OfferScreen.vue` keeps computing its existing happy/sad `reaction` value and passes it to `CharacterFace`, which — per spec — ignores it and renders neutral until 103. New Lobby picker sits in the pre-existing `lobbyCustomiserColumn` placeholder slot (left of the seat circle, mirroring the settings rail on the right — the ticket's "beside the settings rail" placement), with hairstyle/face/hair-colour/face-colour/shirt-colour rows, a live preview, and a Save button wired through a new `setCharacter` App.vue handler/prop chain to the room's existing `setCharacter` message (101). New CSS classes were prefixed `lobbyCharacter*` (not the shorter `character*`) after discovering mid-session that `character*`/`characterPickerLabel` collided with `RolesRevealScreen.vue`'s pre-existing, unrelated Chaser-portrait-picker classes of the same name — confirmed by grep and fixed before commit, called out here since it's exactly the kind of bleed a solo overnight session can introduce silently. Also found and fixed a real (if narrow) Vue reactivity bug while wiring the picker's Save button: an intermediate `myPlayer` object computed made `isDirty` go permanently stale after a successful save, for the exact reason already documented in `App.vue`'s `activeContestantMoney` comment (a player's schema instance keeps the same object identity across state patches, so a computed returning that stable reference never re-fires) — fixed by reading the primitive `character` string directly off `players.value` every access, the same workaround pattern `App.vue` already uses elsewhere. `client build` (`vue-tsc -b && vite build`) and `server test`/`build` (291 tests, no server files touched) both green. Live-verified with two real browser clients through Lobby → RolesReveal → Lineup → CashBuilder → Offer → Chase: picked a distinctive look (hairstyle 5, face 1, purple hair, tan face, then a re-pick to a different hair colour) on the host's own client, hit Save, and confirmed via both a visual check and a DOM/state inspection that the exact same `character` code and rendered avatar appeared on the spectator's client, on the Lineup card, on both clients' CashBuilder profile box, and on both clients' Offer/Chase contestant box — with zero console errors on either client throughout. Two in-scope bugs (the class-name collision and the `isDirty` staleness, both self-introduced this session and described above) were found and fixed during implementation itself rather than shipped; nothing else found, so nothing added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\* 103: implemented unattended overnight per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092/093/098/096/099/100/102's footnotes. Landed the full scope, not a subset. Server: new `REACTION` block in `server/src/gameConfig.ts` (`wrongStreakTear: 2`, plus `offerSadLowThreshold`/`offerHappyHighThreshold` reused verbatim from `OfferScreen.vue`'s former local computed) and a new pure module `server/src/reactions.ts` exporting `nextExpression(prev, correct, streak)`. `TriviaRoom.ts` gained a `wrongStreakBySeat` map (same lifecycle as `sessionIdToSeatId`: cleaned in both `onLeave` branches, and cleared wholesale at the top of every `dispatch()` call since every dispatch is itself a phase/round transition — the ticket's own suggested hook) and three broadcast helpers: `broadcastAnswerReaction` (streak-driven smile/frown/teary, used by cash-builder `submitAnswer`), `broadcastChaseReaction` (used by `resolveChaseQuestion`), and `broadcastSuccessReaction` (smile-only, used by the final round). `reaction { seatId, expression }` now fires at cash-builder `submitAnswer`, every chase-question resolution, a correct `submitFinalAnswer`/resolved-correct `submitFinalStealAnswer`, and both halves of the offer reveal (`setChaserLowOffer`, and the `chaserOffersSet` effect) — every payload carries only `seatId`/`expression`, never an answer or index (verified in tests). Client: `App.vue` added a `reactionsBySeat` ref fed by a new `reaction` `onMessage` handler, each seat's entry auto-reverting to `"neutral"` after `REACTION_HOLD_MS` (a plain client-only constant, following the `CHASER_QUIP_DISPLAY_MS` precedent rather than a literal gameConfig mirror — the ticket's own text says the hold has no server-side equivalent to mirror), cleared on leave; threaded as a `:reactions` prop into CashBuilder/Offer/Chase/TeamFinal/ChaserFinal, each screen looking up its own seat(s) with a `?? 'neutral'` fallback. `CharacterFace.vue`'s `reaction` prop now actually selects eyes/mouth image variants instead of the ticket-102 no-op. `OfferScreen.vue`'s local `reaction` computed and its `HAPPY_HIGH_THRESHOLD`/`SAD_LOW_THRESHOLD` mirror constants were removed outright (the server broadcast fully replaces them) per the ticket's own no-cruft instruction. Judgment calls not spelled out in the ticket: (1) **teary art fallback** — `eyes-teary.png` still doesn't exist at all (confirmed against `HUMAN_TASKS.md` and the actual asset folder), so the `"teary"` state falls back to `eyes-sad.png` for the eyes; for the mouth, paired `"frown"` with `mouth-sad.png` and `"teary"` with `mouth-smirk.png` (rather than reusing `mouth-sad.png` for both) so the two states still read as visually distinct escalation even without the missing teary eyes art — documented in `CharacterFace.vue`'s comments and in updated `HUMAN_TASKS.md` rows; revisit once `eyes-teary.png` lands. (2) **chase reaction has no frown stage** — a single wrong chase answer goes straight to `"teary"` (streak forced to `REACTION.wrongStreakTear` on any chase miss) rather than following the cash builder's 1-wrong-then-2-wrong escalation, on the reasoning that every miss in the chase is immediately high-stakes (the Chaser is actively closing the gap that same question); the chase reaction is broadcast for the active contestant only, per question, never for the Chaser's own seat (the Chaser's portrait stays static, per the ticket's own goal line). (3) **final round reacts to success only** — no frown/teary is broadcast for a wrong `submitFinalAnswer`/`submitFinalStealAnswer`, matching the ticket's own scope line ("correct → smile") and leaning on ticket 100's existing full-screen red flash for negative feedback there instead of layering a face frown on top. (4) **offer reveal's `$0`-middle guard** — `OfferScreen.vue`'s retired computed had an explicit `offer.middle !== 0` guard before frowning on a low `<= 0`; the server-side port needed no equivalent guard because `setChaserLowOffer` structurally can never fire in that case (the $0-middle offer pre-fills `low` to `0`, not `null`, so the handler's existing "already set" guard rejects the call before reaching the new reaction check) — confirmed by an integration test (`ticket 058: a $0 middle...`) continuing to pass unmodified. Added `HUMAN_TASKS.md` clarifications (no new rows — all five reaction assets already existed as rows from ticket 102) noting which are now actually wired in and which still use the documented fallback. Tests: new `server/test/reactions.test.ts` (pure `nextExpression` unit tests: smile on correct regardless of `prev`/streak, frown at streak 1, teary at/above `wrongStreakTear`, and an explicit "`prev` never changes the outcome" case); new reaction assertions added to `server/test/cashBuilderFlow.test.ts` (smile broadcast to both the active and bench clients; frown → teary → smile-resets-the-streak sequence), `server/test/chaseFlow.test.ts` (every reaction during an all-wrong catch run is teary; every reaction during an all-correct escape run is smile), `server/test/roomFlow.test.ts` (four new cases in the existing "chaser sets the offers" describe: low `<=0` frowns, a positive low offer doesn't react, high `>50000` smiles, high `<=50000` doesn't react), and `server/test/finalRound.test.ts` (a correct buzzed team answer and a correct resolved steal each assert the smile broadcast). `cd server && npm test` (304 tests, all green, including every new case), `cd server && npm run build`, and `cd client && npm run build` all green. Live-verified with two real browser clients against the real dev servers (temporary, reverted-before-commit `client/src/App.vue` overrides — shortened pre-cash-builder phase durations and a longer `REACTION_HOLD_MS` for reliable dual-client observation only, confirmed reverted via `git diff` showing zero net change to those lines before commit): a correct cash-builder answer showed the identical `eyes-happy.png`/`mouth-happy.png` pair on **both** the answering client and the Chaser's spectator client at the same instant (checked via direct DOM inspection of the rendered `<img>` sources, not just a screenshot); one wrong answer showed `eyes-sad.png`/`mouth-sad.png` on both; a second wrong answer in a row showed `eyes-sad.png`/`mouth-smirk.png` (the documented teary fallback) on both; letting the hold elapse reverted both clients to neutral (`eyes-1.png`/`mouth.png`) simultaneously; a Chaser-set low offer of `$0` frowned the contestant's face identically on both clients. Confirmed the cue is genuinely server-broadcast, not client-local, since the Chaser's own client (which never submitted an answer) showed the exact same expression as the answering contestant at every step. Offer high-`>$50,000`-smile, chase catch/escape, and final-round smile were not separately re-verified in the live browser beyond the automated integration tests, since they share the exact same `reactionsBySeat`-store → `:reaction` prop wiring already proven live on CashBuilder and Offer, and time was better spent confirming the dual-client sync (the ticket's explicitly-called-out risk) thoroughly on those two rather than repeating the same client-side mechanism a third/fourth time. One unrelated bug was found while live-testing (a real dev-server page reload standing in for a host disconnect) and logged, not fixed: `bug-014` — the host-leaves path's `this.disconnect(6767)` throws an uncaught `TypeError` from the `ws` package because `6767` is outside the WebSocket spec's valid close-code range; pre-existing, unrelated to this ticket's changes.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 104: converted `bug-014` (found live-testing ticket 103, unrelated to it) straight into a ticket and fixed it in the same overnight session, since it's a one-line, low-risk fix for a real production crash path (any host disconnect or page reload threw an uncaught `TypeError` server-side). Changed `TriviaRoom.ts`'s `this.disconnect(6767)` to `this.disconnect(4001)` — `6767` is outside the WebSocket spec's valid close-code range (RFC 6455: 4000-4999 is the private/application-use band `ws@8.21.3` now validates against), `4001` is within it. Updated `AGENTS.md`'s one Gotchas reference to the old code; left the historical ticket files (008/028/042/045/075) that mention `6767` untouched, since they're a record of what was implemented at the time, not live documentation. No test changes needed — `leaveFlow.test.ts`'s host-disconnect tests assert phase/state behavior, not the literal numeric code. `server test` (304 passing) and `server build` green; confirmed via the test log that the "Host left the room — disconnecting …" line no longer has a `TypeError` immediately after it.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 105: investigated the reported hang exhaustively before touching any code — read every call site of `pauseChaserFinalClock`/`resumeChaserFinalClock`/`runChaserFinalCountdown`/`finishFinalSteal` in `TriviaRoom.ts` plus `gameFlow.ts`'s `finalChaserTimeout` case, then stress-tested the pause/resume state machine with scenarios well beyond the single-steal cases 094/095 already covered: three consecutive unclaimed steals, three consecutive steals mixing a claimed pushback/a wrong claim/an unclaimed expiry, and (kept out of the committed suite) an 8-trial randomized torture test mixing dozens of correct/wrong/resolved/expired outcomes per round with jittered timing. All of it passed against the pre-fix code — every dispatch reachable mid-`ChaserFinal` (`finalChaserReachedScore`, `finalChaserTimeout`, `chaserForfeit`) is a terminal transition, so there is no reachable state where `resumeChaserFinalClock`'s own guard silently stalls the clock forever; ticket 094/095's pause/resume machinery was already correct. The real, confirmed bug was narrower and entirely client-side (`bug-013`): `ChaserFinalScreen.vue`'s `secondsLeft` was a plain local `setInterval` with no awareness of the server's real pause/resume, so it kept counting down through every steal window and resolve hold, visibly hit 0:00, and sat there well before the server's own (correctly paused, and therefore genuinely longer) clock actually expired — which reads as "the round doesn't end" even though it eventually does. Fixed by making the server the displayed source of truth: two new synced `GameState` fields, `chaserFinalClockRunning`/`chaserFinalRemainingMs`, mirrored from the room's existing private counters via a new `TriviaRoom.syncChaserFinalClockState()` called from `pauseChaserFinalClock`, `runChaserFinalCountdown` (covers both the initial start and every resume), and `resumeChaserFinalClock`'s already-exhausted branch. `App.vue`'s `onStateChange` mirrors both into refs and only re-stamps a `chaserFinalClockSyncedAt` wall-clock reference when the values actually changed — an unrelated state patch (e.g. a score bump) must not reset the countdown's reference point — then threads all three into `ChaserFinalScreen.vue`, whose `secondsLeft` is now a computed off the real synced remainder: ticking locally (a 250ms `clockNowTick`) only while `chaserFinalClockRunning` is true, otherwise a static read of the frozen remainder. New deterministic regression coverage added to `server/test/finalRound.test.ts` (multi-steal-to-timeout across both unclaimed and resolved resolutions, plus direct assertions on the two new synced fields through start/pause/resume) — the three cases asserting on `room.state.chaserFinalClockRunning`/`chaserFinalRemainingMs` fail without the sync fix (confirmed by temporarily stashing it and rerunning) and pass with it; a fourth (bank-exhaustion-still-times-out) passed even before the fix, which is itself evidence the underlying clock mechanism was never actually broken. `cd server && npm test` (308 passing), `cd server && npm run build`, and `cd client && npm run build` all green. Live-verified with two real browser clients against the real dev servers (temporary, reverted-before-commit `server/src/gameConfig.ts` duration overrides — confirmed reverted via `git diff` before commit): letting the Chaser Final run with no steals correctly ended the round at 0 ("The team made it back"); forcing a Chaser miss showed "Time left" visibly freeze at the exact same number through the whole steal window plus its resolve hold (screenshotted mid-freeze), then resume and tick down by only the genuinely-elapsed running time once the clock actually resumed. Judgment call: no server-side code change was made to the pause/resume/timeout logic itself, since exhaustive testing found nothing to fix there — the fix is the new sync plumbing plus the client rewrite; this is noted here explicitly rather than silently, since the ticket's own hypothesis assumed a deeper server bug would be found.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 106: confirmed the ticket's own diagnosis by reading `client/src/App.vue` before changing anything — `currentQuestion` (the raw ref, ~line 55) is reassigned only by the `"question"` server-message handler's `applyQuestion` (~line 200); nothing ever clears it on a phase change. `currentRoundQuestion` (the computed passed as `:currentQuestion` to both `CashBuilderScreen` and `ChaseScreen`, ~line 149) filtered a non-`"mc"` question only by `targetSeatId !== activeContestantSeatId`, with no phase check at all — so a contestant who continues from their own Cash Builder round straight into their own Chase (the normal case: `cashBuilderTimeout` → `ChaserCharacterReveal`/`Offer` → `contestantChoice` → `Chase` all keep the same `activeContestantSeatId`) keeps satisfying that match, and the stale open-ended question object leaks into `ChaseScreen` until the server's first real `"mc"` question arrives. Confirmed neither `ChaserCharacterRevealScreen.vue` nor `OfferScreen.vue` reads `currentQuestion`/`currentRoundQuestion` at all (grepped both files and `App.vue`'s template — they aren't even passed the prop), so the leak's only visible surface is `ChaseScreen`. Took the ticket's preferred, more-robust fix over the scattered-reset alternative: gated both branches of the `currentRoundQuestion` computed on the existing `currentPhase` ref (already tracked in `App.vue`, reused as-is, no new state) — the cash-builder (non-`"mc"`) branch now also requires `currentPhase.value === GamePhase.CashBuilder`, and the chase (`kind === "mc"`) branch now also requires `currentPhase.value === GamePhase.Chase` — so a stale object from the wrong phase can never satisfy the computed regardless of any future seat-id coincidence. `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests) all green; no server files were touched. Live-verified with two real browser clients against the real dev servers (temporary, reverted-before-commit `client/src/App.vue` room-create-option overrides — shortened `cashBuilderDurationMs`/`chaserRevealDurationMs`/`chaserCharacterRevealDurationMs`/`lineupDurationMs`/`revealReadyCooldownMs` only, plus a temporary console-only debug watch, both confirmed fully reverted via `git diff` before commit) through Lobby → random chaser pick → Cash Builder (timed out with $0 banked, matching ticket 058's $0-middle scenario) → Chaser Character Reveal → Offer (Chaser set an $8,000 high offer) → the contestant picking that offer → Chase, for the same seat throughout. The temporary debug watch logged the raw `currentQuestion` and the fixed `currentRoundQuestion` on every phase change and caught the mechanism directly: `currentQuestion` stayed pinned on the stale Cash Builder object (`{"round":1,"targetSeatId":"seat-2","kind":"open",...,"prompt":"What is the largest mammal in the world?"}`) all the way through `chaserCharacterReveal` → `offer` → `chase`, exactly as diagnosed — while `currentRoundQuestion` correctly stayed `null` in every one of those phases including `chase`. Screenshots taken at the exact instant of the Offer→Chase transition (before the server's async `startNextChaseQuestion()` — which awaits an OpenTDB/local-backup draw — resolved) show `ChaseScreen` rendering "Waiting for the next question…", never the old Cash Builder prompt; once the real chase question arrived a moment later it rendered correctly (prompt + 3 MC options) on both clients. Found and logged one pre-existing, out-of-scope bug while live-testing: `bug-015` (`CashBuilderScreen.vue`'s on-screen "Time left" countdown is a hardcoded `CASH_BUILDER_SECONDS = 60` client constant, disconnected from the room's real, server-authoritative `cashBuilderDurationMs` — reproduced directly, since the test room's 4s override still displayed "Time left: 60s" while the real round ended on schedule a few seconds later) — logged to `BUGS.md`, not fixed, per AGENTS.md's bug-triage rule.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 107: same family/fix as ticket 099 (that ticket explicitly scoped the Cash Builder bubble out, noting it for later). `CashBuilderScreen.vue`'s `bubbleText` now gets its own minimum-life `setTimeout` (`ANSWER_BUBBLE_HOLD_MS = 3000`, same name/value as `TeamFinalScreen.vue`/`ChaserFinalScreen.vue` — no shared module between the per-screen copies, consistent with this codebase's existing duplication pattern for that constant) instead of being cleared from the `currentQuestion` watch — that watch no longer touches `bubbleText` at all (removed from both its question-arrived and question-cleared branches), leaving the bubble to clear itself on its own schedule regardless of how fast the next question arrives. `submit()` clears any pending timeout before arming a fresh one (`clearBubble()`, mirroring `TeamFinalScreen.vue`'s `bubbleText`/`bubbleTimeout`/`clearBubble` pattern exactly), so a same-seat resubmit re-keys cleanly with no leaked/stacked timers; `onUnmounted` now also calls `clearBubble()` alongside its existing interval/timeout cleanup. `style.css` was untouched — pure timing logic, no new classes. `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests, no server files touched) all green. Live-verified with two real browser clients against the real dev servers (no test-only duration overrides were needed — the game's real timing was already fast enough to observe the effect): reached Cash Builder and answered two questions correctly back-to-back; each time the submitted-answer bubble ("Great Wall of China", then "Hadrian's Wall") remained visibly readable *after* the next question had already loaded underneath it, re-keyed cleanly to the new text on the second submission with no stacking/duplication, and then cleared on its own after ~3s with no stuck bubble. Found and logged one pre-existing, out-of-scope bug while live-testing: `bug-016` (the Cash Builder's separate `cashBuilder-flash-correct` full-screen tint appeared to stay stuck on well past its own 700ms clear-timer in one observed run, while the unrelated `bubbleText` timer in the same tab cleared on schedule over the same interval) — logged to `BUGS.md`, not fixed, per AGENTS.md's bug-triage rule.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 108: implemented unattended per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop) — same standing arrangement as tickets 083/092/093/098/096/099/100/102/103's footnotes; the user reported these exact visual problems directly. Read `characterColours.ts`/`CharacterFace.vue`/`style.css` before touching anything, then tried the ticket's own suggested first step (raising `saturate()`/`brightness` in the existing `grayscale→sepia→hue-rotate→saturate→brightness` filter recipe) only on paper — before writing new numbers, sampled the actual placeholder art in a live browser (`getImageData` over `face-1.png`/`hair-2.png`) and found every visible pixel is either fully white or fully transparent, no shading at all. That's a structural dead end for filter tuning: `sepia(1)`'s fixed base hue is what was actually capping vividness, not insufficient `saturate()` magnitude, so this went straight to the ticket's fallback option — a colour-overlay technique — rather than spending the budget on a filter recipe that couldn't reach "clearly distinguishable" no matter the constants. **Tint**: replaced the filter recipe outright. `CharacterFace.vue`'s hair/face layers are now `<div>`s with `background-color` (the target colour, via the existing `characterColourVar`) blended against `background-image: url(art)` via `background-blend-mode: multiply`, with `mask-image`/`-webkit-mask-image` (same art URL, its alpha channel as the mask) clipping the div back to the art's silhouette so the flat colour doesn't paint the whole rectangular box. `multiply` was picked over the more "correct-sounding" `color` blend mode (which keeps the backdrop's luminance and swaps only hue/saturation) only after actually trying `color` first and watching it fail live: at the placeholder's 100%-white luminance there's no headroom left to express any saturation, so every colour blended straight back to white — the exact bug this ticket exists to fix, just relocated into the new code. `multiply` instead darkens proportionally to the art's own value (white × colour = colour exactly), so today's flat-white art reads as the full, undiluted target colour, and it still does the sensible thing once real *shaded* greyscale art lands (white stays full colour, greys darken proportionally into a shadow in the same hue, black stays black) rather than flattening the layer to one flat solid colour the way a silhouette-fill trick would (explicitly ruled out — the ticket warns against a fix that would look wrong once real shaded art exists). Verified programmatically (not just visually) by driving all 9 swatches for both hair and face via real DOM clicks and reading each tint div's computed `background-color`: all 9 resolve to the 9 distinct palette hexes exactly, including confirming charcoal (`#2b2b2b`, near-black) and grey (`#9aa5ab`, mid-grey) — the pair most at risk of collapsing together under a lightness-blind technique — render visibly distinct, since `multiply` (unlike `color`) is naturally sensitive to the source colour's own lightness. The now-dead filter recipe (`CHARACTER_COLOUR_FILTERS`/`characterColourFilter`) was removed from `characterColours.ts` per the no-cruft rule, confirmed by grep to have no other callers. **Head position**: `.offerFaceLayer`'s `top` in `style.css` moved from `-4%` to `2%` — a 6-percentage-point nudge down, ≈13px against the Offer screen's 220px reference box (`.offerContestantMaskBox`), landing inside the ticket's own suggested 8-15px starting range — chosen so the face/hair/eyes/mouth stack's bottom edge (`top` + the layer's fixed `70%` height) lands almost exactly at the shoulders' own top edge (`bottom: 0` + `height: 28%` ⇒ shoulders start at 72%), closing the floating gap without the chin overlapping into the shoulders. Left as a plain percentage (not a fixed px/`translateY`) since every consuming screen already sizes `.offerFaceWrap` via container width/height alone (100%/100%) with no independent transform, so a percentage offset scales identically everywhere by construction, rather than needing a per-screen fixed-px tune. `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests, no server files touched) both green — one `npm test` run hit an unrelated, pre-existing timing-flaky test (`chaseFlow`'s 150ms lockout-window assertion, only 20ms of slack) that passed cleanly on an immediate re-run against the same unmodified code; logged as `bug-017` rather than fixed or ignored, since it's outside this ticket's scope. Live-verified against the real dev servers: cycled all 9 hair and all 9 face colours in the Lobby picker's live preview (screenshotted at 3x zoom, plus the programmatic computed-style sweep above) — every colour reads as a clear, vivid, exact match to its palette swatch, no wash, all 9 clearly distinct from their neighbors. Walked a full two-client game (Lobby → chaser pick → Lineup → Cash Builder → Offer → Chase → Team Final) and spot-checked the head/shoulders fit at 2x-6x zoom on five of the six `CharacterFace` screens (Lineup, Cash Builder, Offer, Chase, and Team Final — the smallest avatar box of the six, `teamFinalAvatarWrap` at 64px, specifically to stress-test the "screen-specific scaling may differ" risk the ticket called out) — every one showed the chin sitting naturally right at the top of the shoulders with no gap and no clipping/overlap, confirming the percentage-based fix scales consistently regardless of the container's absolute size; ChaserFinal (the sixth) was not separately reached in this session but renders through the identical `CharacterFace`/`.offerFaceWrap` markup and CSS already proven on the other five, so it was not considered a distinct risk. No new bugs found in scope beyond `bug-017` above.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 109: re-read `CharacterFace.vue`/`assetPreload.js`/`style.css` fresh per the orchestration-level note that ticket 108 (just landed) had changed the hair/face layers from `<img>` to `background-image` divs — confirmed the eyes/mouth layers are still plain `<img>`, unaffected by 108. Investigated the ticket's own "images without explicit width/height" hypothesis live before writing any CSS: `.offerFaceWrap`/`.offerFaceLayer` already reserve their box purely from CSS (`position: absolute` + percentage `top`/`left`/`width`/`height`, resolved against each screen's own explicitly-sized avatar container — `.offerContestantMaskBox` 220px, `.lobbyCharacterPreview` 120px, `.lobbyBust` 70×63, `.teamFinalAvatarWrap` 64px, `.lineupAvatar` 90px) — confirmed via `getBoundingClientRect()` while every layer's source was still unloaded, hung, or erroring, for both the div-based (108) and img-based layers alike; a layer never collapses to zero size regardless of load state, so that hypothesis didn't hold. Pushed further to find what actually looks broken: forcing a genuinely *pending* (never resolving) load on either an `<img>` or a `background-image`/`mask-image` div paints nothing for that layer — graceful, no flash of an unmasked colour rectangle, no icon — but forcing a real load *error* (404) showed the browser's own broken-image glyph inside the `<img>` eyes/mouth layers despite `alt=""`, while the same failure on a background-image/mask-image div still degrades silently. That inconsistency is the one concrete defect fixed: a new `@error` handler on each `<img>` (`eyesLoadFailed`/`mouthLoadFailed` refs, reset whenever the computed `eyesImg`/`mouthImg` source changes) toggles a new `.offerFaceLayerHidden { visibility: hidden }` class, so a failed `<img>` degrades the same way the div layers already do — box still reserved, nothing jarring painted. The real, verified race: `client/src/assetPreload.js`'s `preloadImages()` fired `new Image().src = ...` with no return value, so nothing could await "preload actually finished" — confirmed live by reading Network activity at Lobby mount and seeing the preload's own image requests and the Lobby picker's first real `<img>`/CSS-background requests for the same files land within milliseconds of each other, a genuine race that only resolves safely because localhost is instant. Fixed by memoizing `preloadImages()` into a single `Promise.all` of each image's `load`/`error` event (resolving, never rejecting, so one broken asset can't hang the batch) and wiring `LobbyScreen.vue` — the earliest and highest-density real consumer of this art — to gate its live customizer preview and every seated player's bust avatar behind a new `assetsReady` ref fed by that same shared promise, rather than gating app boot generally (proportionate to where the stall was actually reported; `App.vue`'s existing fire-and-forget call at module scope is untouched and shares the memoized promise, so this doesn't double the request count). File-size check per the ticket's third question: `face-2.png` (227 bytes) is *not* disproportionately large — it's actually the smallest of the three face variants (`face-1.png` 246B, `face-3.png` 264B); all 16 `CharacterFace` placeholder PNGs sit in a tight 126–303 byte band and the 3 chaser portrait icons in 1.4–1.9KB — noted here as a data observation refuting the ticket's own "large file" hypothesis, not something to optimize (placeholder art, `HUMAN_TASKS.md`). `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests, no server files touched) all green. Live-verified against the real dev servers: reproduced and fixed the broken-image-glyph repro above with real DOM/network manipulation in a live browser tab; cycled all 5 hairstyles and all 3 face options (including face option 2 specifically) in the Lobby picker's live preview with no stall or broken-image flash; played a full two-client game (Lobby → random chaser pick → Lineup → Cash Builder → Offer → Chase) and confirmed via Resource Timing that `eyes-happy.png`/`mouth-happy.png` (the Cash Builder/Chase "smile" reaction art) are already fully fetched and cached from the Lobby-time preload, minutes before Chase ever renders, then confirmed live that swapping them onto the active contestant's face during Chase paints instantly and cleanly with no stall, no glyph, and no layout shift. No new bugs confidently confirmed outside this ticket's scope — one oddity (a Cash Builder text-answer submission not registering on the first synthetic `Enter` keypress during automated testing) was observed but not logged, since it's at least as plausibly an artifact of the test tool's rapid synthetic typing/keydown racing as a real app bug, and Chase's multiple-choice answers (same submission path family, different input) worked reliably throughout the same session.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 110: filled a gap left by ticket 102 (which wired the shared `CharacterFace` component into six screens but missed this one), per a direct user report — implemented unattended per the ticket's own "Proposed direction" (pre-approved by the user at the orchestration level, standing in for the usual AGENTS.md UI sign-off stop), same standing arrangement as tickets 083/092/093/098/096/099/100/102/103/108/109's footnotes. Read `RolesRevealScreen.vue`/`CharacterFace.vue`/`LobbyScreen.vue` before touching anything, confirming the ticket's own framing: the screen's `characterPicker`/`characterPickerLabel`/`characterOptions` markup (~lines 37-50) is the pre-existing, unrelated Chaser roster-portrait picker (`CHASER_CHARACTERS`/`ChaserCharacter` from `gameConfig.ts` — Bezos/Big Stan/Nami), distinct from a contestant's custom `character` codec avatar; the contestant list lives separately, in `.revealContestants`/`.contestantCard`. Swapped that card's placeholder — a plain div rendering the player's first initial (`{{ player.name.charAt(0).toUpperCase() }}`) — for `<CharacterFace :character="player.character" reaction="neutral" />`, matching the exact wrapper convention already established by `ContestantLineupScreen.vue`'s `.lineupAvatar` (a `position: relative`, fixed-pixel-sized, `overflow: hidden` circle housing `CharacterFace`, which itself fills via `.offerFaceWrap`'s `100%`/`100%`). `style.css`'s `.contestantAvatar` rule was rewritten accordingly: kept its `60px`/`60px`/`50%` sizing exactly (matching the existing row layout, per the ticket's own scope note — this was a small addition, not a redesign) but dropped the now-dead letter-avatar declarations (`color`, `font-family`/`weight`/`style`/`size`, the centering `display: flex`/`align-items`/`justify-content` — all only ever needed for the removed single-character text) and added `position: relative` (required as the containing block for `CharacterFace`'s internal absolutely-positioned layers) and `overflow: hidden` (clips the layers to the circle, consistent with `.lineupAvatar`/`.lobbyBustAvatar`); swapped the placeholder's `background-color: var(--color-azure)` for `var(--color-grey-avatar-bg)`, the same neutral avatar-frame token those two classes already use, since the azure fill was specifically chosen to contrast with white letter text that no longer exists. Deliberately did not add `LobbyScreen.vue`'s `assetsReady`/`preloadImages()` gate here — grepped every other `CharacterFace` consumer (`ContestantLineupScreen`, `CashBuilderScreen`, `OfferScreen`, `ChaseScreen`, `TeamFinalScreen`, `ChaserFinalScreen`) and confirmed none of them gate on it either; ticket 109's own footnote is explicit that the gate is proportionate to where the image-preload stall was actually reported (Lobby specifically), not a general requirement for every screen rendering the component. The Chaser's own picker markup, `ChaserCharacter`/`availableCharacters`/`handleCharacterSelect`, and its CSS (`.characterPicker`, `.characterOptions`, `.chaserImage`) were not touched at all — confirmed by diff. `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests, no server files touched) both green. Live-verified with three real browser clients against the real dev servers: gave the host a distinctive look (hairstyle 5, face 2, green face/purple hair-and-shirt) and a second contestant a differently distinctive one (hairstyle 4, face 3, yellow face, blue hair, yellow shirt) in the Lobby, then started the game — the random chaser pick landed on the second-joined player, so the *host* ended up a contestant on this run. On Roles Reveal, the host's real green/purple avatar (not a placeholder, not nothing) rendered correctly inside `.contestantAvatar` on all three observed clients: the Chaser's own client, the host-turned-contestant's own client, and a third client that joined after the game started (added to the contestant list rather than treated as a distinct spectator role, since this room has no separate spectator flow yet — still confirms the avatar renders for a non-participant late joiner). The Chaser's roster-portrait picker was independently confirmed unaffected and still fully functional on the Chaser's own client: clicking a portrait correctly emitted `ready`, flipping the screen to the normal "waiting" silhouette view with a green ready-check next to the Chaser's name, exactly as before this change. Investigated one visual oddity before ruling it out as in-scope: the Chaser's own portraits render as solid black silhouettes by default (colour revealed only on hover) — confirmed via Network activity that all three `chasers/*-icon.png` files load `200 OK`, and via `style.css` that `.chaserImage { filter: grayscale(100%) brightness(0%) }` / `.chaserImage:hover { filter: grayscale(0%) brightness(100%) }` is a pre-existing, intentional "unrevealed until picked/hovered" design, not a bug or a regression from this change. No bugs found in scope; nothing added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 111: investigated empirically before touching any CSS, per the ticket's own required process — walked the phase screens live in the browser (two/three real Colyseus clients, fixed 1280×900 viewport, temporary reverted-before-commit `client/src/App.vue` room-create-option overrides shortening every phase duration for observation only, confirmed reverted via `git diff` before committing), then, where live timing made repeated precise capture impractical (a steal window's exact open instant), cross-checked with direct `getBoundingClientRect()`/`getComputedStyle()` measurements against the real, already-loaded `style.css` and the exact class names/markup copied verbatim from each source file — not guessed-at values. Landed mid-session while a separate, concurrent design-system conversation (tickets 115–119, `tickets/067`'s new outcome note) was reaching agreement on a full visual redesign of four of these same screens; per that overnight update, keeping this ticket's footprint minimal and CSS-first (no markup restructuring) took priority over a broader sweep, since 116/117 will shortly rewrite large parts of `ChaseScreen.vue`/`ChaserFinalScreen.vue`'s styling anyway. Four concrete, root-caused shift-causes found and fixed, each confirmed live to actually move something before fixing it, and reconfirmed live or via direct measurement after:
1. **`ChaseScreen.vue`'s off-board note** (`.chaseOffboardNote`, the "The Chaser is off the board — one correct answer to enter." line) was `v-if`'d in and out of the DOM as `chaserIsOnBoard` flips mid-round — confirmed live with real screenshots: the moment the Chaser's first correct answer put them on the board, `.chaseTableRow` (both portraits + the board) visibly jumped up ~38px to fill the note's vacated space. Fixed by always rendering the paragraph and toggling a new `.chaseOffboardNote-hidden { visibility: hidden }` class instead of removing it from the DOM, so its line is permanently reserved (`ChaseScreen.vue` line ~184, `style.css`).
2. **`ChaseScreen.vue`'s question prompt** (`.chaseQuestion`) had no `min-height`/`line-height`, unlike every sibling question box in the app (`.cashBuilderQuestionArea`'s fixed 175px, `.teamFinalQuestionArea`'s 160px min-height) — confirmed via direct DOM measurement with the real element and real question text that a 1-line vs. a 3-line prompt changed `.chaseQuestion`'s own height from 24px to 72px, dropping the answer-button row below it by 48px; this recurs on every single chase question, not just once per round. Fixed with `line-height: 1.3; min-height: 52px;` (2 lines' worth) on the existing `.chaseQuestion` rule — pure CSS, no template change.
3. **`ChaserFinalScreen.vue`'s top slot** (`.chaserFinalTop`, which swaps `ChaserPanel` for `.chaserFinalStealQuestion` via the pre-existing `v-if="stealActive"`) had no reserved height at all — confirmed via a same-markup synthetic DOM test against the real stylesheet that the swap alone dropped `.finalTargetRow`/`.chaserFinalBottom` below it by ~200px (repeats on every steal open/close in a round), and live-measured the real `ChaserPanel` state at 294–300px tall in-game. A real live steal wasn't caught in time before the (deliberately shortened) Chaser Final round ended in this session's playthrough, so the fix's zero-shift result is confirmed by direct measurement against the live stylesheet with the exact real markup for both states, not by eye in a live steal. Fixed with `align-items: center; min-height: 300px;` added to the existing `.chaserFinalTop` rule (`style.css`) — centers whichever side is showing within a reserved slot; no template change, and 117 (which explicitly plans to "restyle both sides of it") can freely replace this rule without needing to know it existed.
4. **`CashBuilderScreen.vue`'s trailing status line** (`.cashBuilderStatus`, "Time's up!"/"Next question…") is `v-if`/`v-else-if`'d at the end of nearly every question — `.cashBuilderBody`'s `align-items: center` recentres its two flex children (the question column and the profile-box/`CharacterFace` column) whenever one's height changes, so this line appearing/disappearing visibly shifted the profile box vertically; confirmed by direct DOM measurement with the real markup (32px shift pre-fix). Fixed by wrapping both occurrences (the active-contestant branch, `CashBuilderScreen.vue` line ~231, and the spectator branch, line ~245) in a new `.cashBuilderStatusSlot` wrapper div that always renders, plus a `min-height: 44px` rule sized to the status `<p>`'s own real rendered height. Getting this to a true zero-shift took two iterations, both confirmed by re-measuring: a plain `min-height` on the slot let the `<p>`'s default 20px browser margin collapse through it since the slot has no border/padding, overshooting the reservation; switching the slot to `display: flex; align-items: center; justify-content: center` stopped the collapse but then counted the same margins toward the flex item's own outer size instead (84px, not 44); zeroing `.cashBuilderStatus`'s own margin (safe — grepped and confirmed the class is used nowhere else) alongside the flex slot finally landed on an exact, reconfirmed 0px shift across all three states (empty / "Time's up!" / "Next question…").

Deliberately left alone, and why: (a) `OfferScreen.vue`'s `.offerControls` block changes height as the Chaser progresses through low→high→"offers sent" (and the `offer.middle !== 0` 2-vs-3-button tier row from ticket 058) — investigated and ruled out as in-scope, since it's the last element on the page with nothing below it to push around; this reads as sequential game-state content changing, not a stability bug, so it was left untouched rather than papered over. (b) `RolesRevealScreen.vue`'s Chaser-only picker→silhouette swap is a real, one-time, per-viewer state transition (picking a character), not a load/content-length artifact — same reasoning, left alone. (c) Per the orchestration-level update mid-session, did **not** touch anything in `ChaseScreen.vue`'s board/countdown/cutscene presentation, `ChaserFinalScreen.vue`'s banner/steal-box chrome, `TeamFinalScreen.vue`'s table, or `LobbyScreen.vue`'s seat circle/settings beyond the four surgical rules above — ticket 116 (Chase board), 117 (Chaser Final + Steal), 118 (Team Final), and 119 (Lobby) are about to redesign exactly these screens' *look*, and 116 explicitly already commits to its own new question-box entrance sequence never reflowing anything, so a broader speculative pass here would only add rework/merge-conflict surface for no lasting benefit. `client/src/style.css`'s existing `.teamFinalQuestionArea`/`.chaserFinalBottom`/`.cashBuilderQuestionArea` were confirmed, not touched — they already reserve space correctly from earlier tickets. No screen was found where the actual *design* (as opposed to stability) looked like the problem beyond what 067/115-119 already cover. `cd client && npm run build` and `cd server && npm test` (308 tests) / `npm run build` both green throughout (no server files touched). No new bugs found in scope; nothing added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 112: fixed `bug-015` — `CashBuilderScreen.vue`'s "Time left" counted down from a hardcoded `CASH_BUILDER_SECONDS = 60`, completely disconnected from the room's real (possibly clamped/overridden) `cashBuilderDurationMs`. Checked first whether the room already exposed the effective duration to clients, per the ticket's own instruction — it didn't: `room.cashBuilderDurationMs` (set once in `onCreate` via `clampRoomOptions`) never appeared in any synced `GameState` field, join-option echo, or broadcast message, so a new field was needed rather than reusing an existing one. Followed `ChaserFinalScreen.vue`'s ticket-105 precedent of deriving a client display from a real synced `GameState` field instead of a hardcoded guess, but simpler per the ticket's own note: since the duration doesn't change mid-game (unlike the Chaser-final clock's pause/resume), one plain `@type("number") cashBuilderDurationMs` field (`GameState.ts`, defaulting to `CASH_BUILDER.durationMs`) set once in `TriviaRoom.onCreate` right after `clampRoomOptions` runs is enough — no running/paused pair, no broadcast message needed. `App.vue` mirrors it into a ref on every `onStateChange` (alongside the other synced fields) and threads it into `CashBuilderScreen.vue` as a new `cashBuilderDurationMs` prop; the screen's `secondsLeft` ref now initializes from `Math.ceil(props.cashBuilderDurationMs / 1000)` instead of the removed `CASH_BUILDER_SECONDS` constant — safe because the component is freshly mounted (`v-if`) each time a contestant's cash-builder round begins, so the prop already holds the correct real duration at mount, matching the ticket's "just a correct starting value" scope (no ongoing sync needed once the countdown starts). `cd client && npm run build`, `cd server && npm test` (308 tests, unaffected) and `cd server && npm run build` all green. Live-verified with two real browser clients against the real dev servers (temporary, reverted-before-commit `client/src/App.vue` room-create-option override — `cashBuilderDurationMs: 8000` added to the `client.create` call only, confirmed reverted via `git diff` before committing): created a room with the 8-second override, played through to Cash Builder, and watched the spectator's screen start from and count down a real ~8-second budget (captured mid-round at "Time left: 4s") instead of the old fixed 60, with the round's actual server-driven end time unaffected either way. `BUGS.md`'s `bug-015` flipped to resolved, citing this ticket. No new bugs found in scope; nothing else added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 113: investigated `bug-016` (the Cash Builder correct-answer green flash allegedly staying stuck well past its 700ms auto-clear) per the ticket's own required first step — ruled out a testing artifact before touching any code. Read `CashBuilderScreen.vue`'s `answerResult` watch fresh (re-checked against ticket 112's just-landed `cashBuilderDurationMs`/`secondsLeft` changes, which don't touch this logic): every invocation already unconditionally `clearTimeout`s any pending `screenFlashTimeout` before a correct answer re-arms it, so back-to-back correct answers only ever re-arm the same single timer — there is no code path where it gets overwritten without being cleared, so no plausible in-code race was found on inspection alone. Live-verified with two real browser clients against the real dev servers (temporary, reverted-before-commit `client/src/App.vue` room-create-option override — `cashBuilderDurationMs: 300000` added to the `client.create` call only, confirmed reverted via `git diff` before committing, so there'd be enough real time to chain several answers without the round timing out mid-test): played as the sole contestant with the Cash Builder tab kept genuinely foregrounded for the entire session (never switched away, unlike the original two-tab repro), answered 7 questions correctly in a fast-chained row (looking up each correct answer from `server/data/questions.json` to keep the pace tight), and screenshotted immediately after each submission plus again a few seconds later — the green flash appeared right on submission every time and was reliably gone within at most ~2-3s, never stuck. Concluded not reproducible with a genuinely foregrounded tab: the original report's own multi-tab setup (with the browser's background-tab timer throttling in play) is the far more plausible explanation, exactly as the ticket's first-step hypothesis anticipated. No code changes made (nothing to fix for a non-reproducible artifact); `client/src/App.vue`'s temporary test override confirmed reverted via `git diff` (clean). `BUGS.md`'s `bug-016` closed as a non-reproducible testing artifact, citing this ticket. No new bugs found in scope; nothing added to `BUGS.md` beyond `bug-016`'s own status update.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 114: fixed `bug-017` — `chaseFlow.test.ts`'s "lockout window" test asserted `Date.now() - startedAt >= windowMs - 20` around a real `chaseAnswerWindowMs: 150` round-trip, which failed intermittently under incidental system load (observed: resolved after 116ms against a 130ms floor). Read `timer.test.ts` first per the ticket's own pointer: its "controllable clock" pattern is a bare `TimerRoom` wrapping its own `ClockTimer` that the test constructs and waits on directly, exercising `scheduleTimer` in isolation — it doesn't transplant onto this test, because `chaseFlow.test.ts`'s lockout test drives a real Colyseus room created via `colyseus.createRoom()`, whose `room.clock` is the framework's own auto-ticking clock wired through actual network message dispatch (`submitChaseAnswer` round-trips over the wire exactly as in production); there's no seam to inject a manually-advanced virtual clock there without giving `TriviaRoom` an injectable clock, which would be a production-code change and out of this test-only ticket's scope. Widened the wall-clock margin instead: `windowMs` raised from 150ms to 500ms (matching the window already used by the neighboring `chaseLockoutStarted` test) and the lower-bound assertion changed from a fixed `windowMs - 20` to a proportional `windowMs * 0.6` (300ms) — big enough to absorb the kind of jitter observed, while still comfortably distinguishing "waited out the lockout" from the sibling "resolves immediately once both sides have answered" test's single-digit-to-low-double-digit-ms resolution. No production code touched (confirmed via `git diff --stat`: only `server/test/chaseFlow.test.ts` changed). `cd server && npm test` run 5 times in a row on the changed code: 308/308 passing every run, the lockout-window test itself green each time (~1310-1318ms, no flake observed); `cd server && npm run build` green. `BUGS.md`'s `bug-017` flipped to resolved, citing this ticket. No new bugs found in scope; nothing else added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 115: implemented unattended per the user's explicit direction to proceed with the already-agreed mockup ("Big Baws Style Guide") without a fresh UI sign-off round — the mockup itself (read directly for exact values) took precedence over this ticket's own prose wherever the two seemed to disagree, per the ticket's own instruction. CSS/markup-only, `client/src/style.css` + `client/index.html`, no `.vue` changes (116-119 wire this into real screens). Added `--font-display`/`--font-pixel`/`--font-body` tokens (Luckiest Guy / VT323 / Rubik) and re-typed all ~30 existing `font-family: "Roboto"` declarations plus the `h1,h2,h3,h4` base rule individually by role rather than a blanket replace — e.g. `.cashBuilderPot`/`.teamFinalPotAmount` (standalone big-number displays) went Luckiest Guy matching the new `.moneyPlaque .amount` treatment; `.cashBuilderTimer`/`.chaserFinalStealCountdown`/`.chaseWagerBadge`/`.offerSpaceAmount`/`.boardSpaceNumber`/`.offerSpaceNumber`/`.finalTargetBox`/`.lineupOrdinal` (timers, countdowns, board-space numbers/wagers) went VT323; `.cashBuilderRevealLabel`/`.teamFinalRevealLabel`/`.chaseOutcomeBanner`/`.resultsBanner` (reveal/outcome banners) went Luckiest Guy; `.wheelSlot` (a candidate name inside the chaser spin-reveal) went VT323, matching the mockup's own `.chaser-pick span` precedent, distinct from plain roster names (`.contestantName`/`.voteName`, which stayed Rubik); everything else (buttons, inputs, body copy, questions, names) went Rubik. Also added an explicit `font-family: var(--font-pixel)` to `.roomCode` (an `h3`, previously inheriting the base rule) since the ticket names room codes as a VT323 example on their own. Left `.chaserFinalScore`/`.teamFinalScore`'s prose HUD line ("Time left: Ns, Team score: N") as inherited Rubik rather than forcing VT323 onto it — the mockup's actual `.cf-hud` treatment (whole line in pixel font, gold-bolded numbers via a nested tag) needs a markup restructure that belongs to tickets 117/118, not this CSS-only pass. `client/index.html`'s Google Fonts link now loads Rubik at weights 400/500/600/700/800 (the ticket's own literal list; skipped the mockup's extra italic-500 variant since nothing here uses italics) and drops Roboto outright — once every declaration was re-typed, Roboto was fully orphaned by this ticket's own work, so removing it followed AGENTS.md's no-cruft rule rather than leaving a dead font load. Colour tokens: added `--chaser-deep`/`--chaser-edge`/`--ink-chase`/`--ink-chase-raised` verbatim per the ticket, plus `--ink-raised`/`--ink-line`/`--btn-ink` (not spelled out by the ticket's prose but needed by the components it asks for — the open-question-box family's background/border and the new button family's border/text ink respectively — added per AGENTS.md's "tokenise a repeating colour" rule) and a `--color-green-rgb` companion (this file's existing convention: every solid colour used in `rgba()` gets a `-rgb` twin) for the money-plaque's correct-answer wipe. Judgment call on `--success-green`: the ticket lists it as a token to add but also says to reuse existing names/values rather than duplicate them, and its value is byte-identical to the pre-existing `--color-green` — resolved by adding `--success-green: var(--color-green);`, an alias rather than a second literal, so 116/117 can reference the mockup's own semantic name without a duplicated value. Built the components verbatim to the mockup's own class names (including camelCase `.moneyPlaque`, not kebab-cased) since the ticket and 116-119 both refer to these by exact name: the flat-press family (`.btn`/`.btn-primary`/`.btn-outline`); the raised-3D-press family (`.buzzer-btn`, `.lobby-start-btn` plus `.lobby-play-triangle`, `.answer-btn` plus persistent `.picked`/`.dimmed`); frame-free circular portraits (`.board-portrait-wrap`/`.board-portrait-circle`/`.board-portrait-img`, `.chaser-wrap` for the 20%-bigger static variant — no growth/interaction mechanic, matching the mockup's own final call after trying and dropping one); the money plaque (`.moneyPlaque` plus `.who`/`.amount`, `.plaque-wipe`/`.wiping`, `.plaque-squeeze`/`.squeezing`); the ticket badge (`.ticket-badge` plus punched `::before`/`::after` notches, `.divider`, `.code`); the open-question-box family, all 4 accents (`.open-question-box.accent-red/-gold/-blue/-neutral` plus `.oq-eyebrow`/`.oq-prompt`/`.oq-row`/`.oq-input`/`.oq-submit`); the particle-burst primitives (`.bill`/`.spark` sharing one `bill-pop` keyframe, driven by caller-set `--dx`/`--dy`/`--rot`); and the head-wobble/eyes-squeeze reaction (`.bust-pokeable`/`.poked` as the generic trigger this foundation ships, plus the unscoped `.head`/`.eyes` base rules and `head-wobble`/`eyes-squeeze` keyframes) — ported the mockup's own comment explaining the positioning bug it fixes, and noted in-code that `CharacterFace.vue` doesn't have a `.head` wrapper yet (its layers sit directly in `.offerFaceWrap`), so whichever of 116-119 wires this reaction into real characters needs to add one grouping face/hair/eyes/mouth only (shoulders stay planted, outside `.head`). Deliberately did not port `.oq-thinking`/`.oq-dots`, `.cf-hud`, `.tf-*`, `.chase-stage`/`.catch-stage`/`.escape-stage`, or any `.board-demo-*` layout wrapper — those are screen-specific markup for 116-119's actual screens, not foundation primitives this ticket's own summary calls out. Real bug found and fixed in scope (not logged to `BUGS.md`, since it's inside this ticket's own new code): the pre-existing generic `button { width: 350px; height: 75px; }` rule silently wins over any new class that doesn't itself declare width/height — a class selector only beats that element selector for properties both declare, and `.btn`/`.answer-btn`/`.oq-submit` (relying on padding, like the mockup) don't set dimensions at all, so without an explicit `width: auto; height: auto;` override on each, every future flat-press button, Chase-board answer button, and open-question-box submit button would have silently rendered as a 350x75 pill instead of the mockup's content-sized chunky button — confirmed live (computed style showed exactly 350x75 before the fix) and fixed before any 116-119 implementer could hit it. `prefers-reduced-motion` mirrors the mockup's own asymmetric split exactly (not an inconsistency introduced here): `.bill`/`.spark`/`.plaque-squeeze.squeezing`/`.bust-pokeable .head`/`.bust-pokeable .eyes`/`.moneyPlaque.wiping .plaque-wipe` lose their animation, and the raised-3D-press family (`.buzzer-btn`/`.lobby-start-btn`/`.answer-btn`) also loses its press transition (a much larger, more physical travel than the flat-press family's small hover/active nudge, which the mockup leaves animated even under reduced motion). `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests) all green. Visually verified every new component live against the Vite dev server via a temporary scratch component (mounted in place of `App` by editing `main.ts`, both reverted/deleted before committing — confirmed via `git diff`/`git status` showing zero residual trace): flat-press hover lift, raised-3D press-down plus lip, `.picked`/`.dimmed` persistence, frame-free circular crop at both sizes, the money plaque's click-triggered wipe plus tick-up ($4,500 to $4,800) plus squeeze plus dollar-burst (confirmed the burst's DOM/CSS animation directly since screenshot timing kept racing its 0.85s lifetime), the ticket badge's punched notches, all 4 open-question-box accents, and the head-wobble/eyes-squeeze reaction — polled `.head`'s computed transform and `.eyes`' bounding box through the animation window and confirmed the rotation genuinely animates and the eyes' height oscillates (29 to 9 to 33 to 28px) without ever collapsing to zero, i.e. the ported positioning-gotcha fix actually holds under animation, not just at rest. Also spot-checked an untouched screen (Home) for regressions: renders correctly with only the expected sitewide font swap, no layout changes. No entries added to `BUGS.md`.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 116: implemented unattended per the user's explicit direction to proceed with the already-agreed mockup without a fresh live UI sign-off round (the ticket's own acceptance criteria still nominally call for one — noted here, not silently dropped). Read the mockup's section 11 ("The Board"), the two cutscene sections (09/10), and the full `feedback_visual_direction.md` history (26 rounds, several explicit reversals) before touching anything, per the ticket's own warning about repeating earlier mistakes. `client/src/screens/ChaseScreen.vue`: replaced `.chaserPanelMask`/`.offerContestantMaskBox`-style boxes with 115's frame-free `.board-portrait-wrap`/`.board-portrait-circle`/`.chaser-wrap`; dropped `boardSpaceNumber` and `.chaseEscapeSpace` entirely (template + CSS); moved `chaseWagerBadge` onto the contestant's own space (VT323, white, centered — `chaseWagerAmount`/`formatAmount` untouched); restyled the trail flat (`chaserSpace` → `--chaser-deep`, no gradient) with only `.boardSpace-current` bright+glowing (dropped the old white/red *border* on current entirely, matching the mockup's "brightness only" rule); added a new `.chaseGround` two-toned (`--ink-chase` → `--color-bg-page`, 32%/68% stops) wrapping card replacing the old bare `.chaseTableRow`; gave the question area a real card (`.chaseQuestionBox`, neutral `--ink-raised`) with the 3-beat entrance sequence (CSS `clip-path` wipe + a scene-wide `.chaseWipeBar` sweep, both keyed on `currentQuestion.questionId` so they replay every question via Vue's own remount rather than JS class-toggling); switched the answer buttons from `.startButton.chaseOptionButton` to 115's `.answer-btn`, with `.picked` persisting and `.dimmed` on the other two (bare classes, matching 115's own convention) — relocated the pre-existing `chaseOptionButton-correct`/`-wrong`/`-fontSm`/`-fontXs` modifiers into a new trailing "Chase board redesign" CSS block *after* `.answer-btn`'s own rules specifically so they keep winning cascade ties now that they're compounded onto a class defined near the end of the file, not because their behavior changed. Caught/Escaped got a real cutscene (`.chaseCutscene(-caught/-escaped)`) swapped in for the board+question area while `chaseOutcome` is set: the Chaser (a bare `<img>`, scaled to ~190px, frame-free) slams in/lunges, the contestant (reusing `<CharacterFace>`) flies off/dashes away and fades, a white flash + screen-shake on Caught, a reused-from-115 `★` `.spark` burst (JS-spawned, guarded behind a `prefers-reduced-motion` check — see bug note below) on Escaped; the existing `.chaseOutcomeBanner`/`chase-outcome-pop` `Transition` is completely unchanged, just delayed ~500-650ms via a new local `showOutcomeBanner` ref so the impact beat visibly lands *before* the banner rather than under it.

Judgment calls not fully spelled out by the ticket: (1) **`ChaserPanel.vue`** — rather than fork a second component or change the shared box look (it's also used, unmodified, by Offer and the Chaser Final — tickets outside this one's scope), added two purely-additive props, `circlePortrait` (default `false`) and `countdownSeconds`/`countdownUrgent`, gated behind a `v-if`/`v-else` branch in its template; Offer/Chaser Final pass neither prop and render byte-identical to before, only `ChaseScreen.vue` opts in. (2) **Chaser portrait/name lookup** extracted from `ChaserPanel.vue` into a new tiny shared module, `client/src/chaserPortraits.ts`, so the cutscene's bare `<img>` (which needs the same icon outside `ChaserPanel`'s own markup) doesn't duplicate that table — `ChaserPanel.vue` now imports from it too, no behavior change. (3) **Countdown badge side selection** — the ticket says the badge goes on "whichever side is actually answering," but `chaseLockoutStarted` is deliberately anonymous server-side (see `submitChaseAnswer`'s own comment in `messageHandlers.ts`: it never says who answered first, "so the side still deciding never learns who answered or what they picked"). Resolved without any server change or new leak: a participant already knows their own `hasAnswered` locally, so they can work out the other side's status by elimination (lockout only starts once exactly one side has answered) — `lockoutBadgeSide` computed in `ChaseScreen.vue` does exactly that (chaser sees it on their own circle if they haven't answered, else on the contestant's; symmetric for the contestant); a non-participant spectator has no such local knowledge, so both circles show the badge for them, which is honest (genuinely ambiguous to them) rather than a guess. Flagged explicitly here since it's a real design decision the ticket's prose didn't anticipate. (4) **Roster name font** — kept `.chaserPanelName`/the contestant's name label in Rubik rather than switching to VT323 like the mockup's own `.board-demo-side-name` demo detail, since ticket 115's own footnote already made and documented a considered, general call that plain roster names stay Rubik sitewide (`.contestantName`/`.voteName`) and only HUD-ish bits (timers, wagers, room codes) go VT323 — favoured that standing internal-consistency decision over one demo section's specific styling. (5) Did **not** add a `.head` wrapper to `CharacterFace.vue` — this ticket's scope doesn't put the poke/wobble reaction on the board (only the cutscenes' fly/dash/fade, which are plain CSS transforms on the existing markup), so the gap 115's footnote flagged (`CharacterFace.vue` has no `.head` grouping yet) is still open for whichever ticket actually needs that reaction on a real character. (6) Left `.chaseLockoutFlash`/`chase-lockout-bg`/`--color-lockout-blue-rgb` fully defined in `style.css` — only removed *ChaseScreen.vue's own* use of them (per the ticket's explicit instruction to kill the full-screen flash here) — because `ChaserFinalScreen.vue`'s steal flash (ticket 096) still reuses that exact class/keyframe by name; deleting it would have broken an unrelated, out-of-scope screen. **Note for ticket 117**: that reuse is still there and unexamined — 117 should decide whether the Chaser Final keeps riding on `.chaseLockoutFlash` or gets its own redesigned treatment, and can reuse this ticket's `circlePortrait`/`countdownSeconds` props on `ChaserPanel` plus the `.countdown-chip`/`.chaseCountdownBadge` classes directly for its own "imposing Chaser" + countdown needs rather than reinventing them. Real (small, in-scope) bug caught and fixed while building the Escaped cutscene, not logged to `BUGS.md` since it's inside this ticket's own new code: 115's `prefers-reduced-motion` block disables `.spark`'s *animation* but (unlike `.bill`, which also gets `opacity: 0`) doesn't hide it outright — since the spawner relies on the `animationend` event to remove each spawned `<span>`, a disabled animation under reduced motion would leave stars permanently stuck on screen; fixed narrowly in `ChaseScreen.vue`'s own spawner (skips spawning entirely under `prefers-reduced-motion`) rather than touching 115's shared CSS. No other bugs found in scope; nothing added to `BUGS.md`. `cd client && npm run build` (`vue-tsc -b && vite build`) and `cd server && npm test`/`npm run build` (308 tests, zero server files touched — confirmed via `git status`/`git diff --stat`) all green. Live-verified with three real browser clients (temporary, reverted-before-commit overrides: `client/src/App.vue`'s `client.create` options shortened `cashBuilderDurationMs`/`chaserRevealDurationMs`/`chaserCharacterRevealDurationMs`/`revealReadyCooldownMs`/`lineupDurationMs`, and `CHASE_RESULT_HOLD_MS` was temporarily raised from 2500 to 9000 to give a slow manual playthrough room to actually observe the cutscene instead of racing past it — both reverted, confirmed via `git diff` showing zero residual trace before committing) through two full games: a High-offer round where the contestant answered wrong and the Chaser answered right twice (real trivia, not scripted) resolved to **Caught**, the board correctly showing the flat dark-red trail behind the Chaser's bright glowing current space and the contestant's wager centered on their own space the whole time, followed by the slam/flash/shake cutscene and the delayed red `CAUGHT!` banner; a Middle/High-offer round where the contestant answered six real trivia questions correctly while the Chaser was left unanswered each round (confirmed via computed styles that the Chaser genuinely never entered the board — the offboard note stayed visible throughout) resolved to **Escaped**, with the dash/fade cutscene and delayed blue `ESCAPED!` banner. Confirmed directly via DOM/computed-style inspection (not just visual screenshots, several of which raced the entrance sequence or a mid-flight cutscene frame and looked misleadingly empty): the full-screen `.chaseLockoutFlash` never renders on this screen any more, `.boardSpaceNumber`/`.chaseEscapeSpace` never render at all, the entrance sequence's buttons are genuinely `opacity:0`/`visibility:hidden` (not absent) during the 3s prompt-alone beat and pop in with the real question text afterward, the countdown badge appears on the correct circle from both a participant's and (showing on both circles) a spectator's point of view, and `.picked`/`.dimmed` apply to the exact expected buttons through both the mid-round and revealed states. No console errors on any of the three clients related to this ticket's changes (one benign "WebSocket is already in CLOSING or CLOSED state" logged by a tab after it had already navigated away from a since-abandoned earlier test room — expected SDK cleanup noise, not a regression).

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 117: implemented unattended per the user's explicit direction to proceed with the already-agreed mockup ("Big Baws Style Guide", sections 06/12/14) without a fresh live UI sign-off round — this direction was reached through extensive prior back-and-forth already documented in `feedback_visual_direction.md` (rounds ~3/10/14/15 specifically cover this screen). Read `ChaserFinalScreen.vue`/`ChaserPanel.vue`/`chaserPortraits.ts` fresh first, per the ticket's own instruction, since the screen has grown across tickets 094-105 (clock sync, steal transport, answer bubbles) — none of that logic was touched; confirmed via diff that `stealLocked`/`finalStealAnswer`/`finalStealResolved` handling, `chaserFinalClockRunning`/`chaserFinalRemainingMs`, the steal-resolve-hold countdown, and `submitChaserAnswer`/`submitSteal` message shapes are byte-identical to before.

**`ChaserPanel.vue` reuse decision** (the question ticket 116's footnote left open): added a *third* portrait mode, `bannerPortrait` (default `false`, alongside the existing `circlePortrait`), rather than reusing `circlePortrait` for the imposing Chaser-Final banner — the mockup's `.chase-stage-chaser`/`-img` is a bare, frame-free cutout with a big drop-shadow (mirroring `ChaseScreen.vue`'s own Caught/Escaped cutscene `.chaseCutsceneChaser` treatment, resolved via the same shared `chaserPortraits.ts` lookup), not a circle — visually a different language from the board's frame-free *circle*. Kept `ChaserPanel` as the one shared component regardless (per the ticket's own steer): the bubble/name/input-row markup is identical across all three modes, so `bannerPortrait` only swaps the portrait's own inner markup (new `.chaserStageBanner`/`.chaserStageBannerImg` classes) plus a `.chaserPanelName-banner` modifier on the existing shared name `<p>` for the VT323 treatment — `circlePortrait`/`countdownSeconds` (116's own props) are untouched and still exclusively used by `ChaseScreen.vue`; Offer still renders the original default box, byte-for-byte.

**The "Watching" state — confirmed a real, small server change was needed**, not a template-only one: checked `TriviaRoom.sendFinalQuestion` fresh (ticket 077) and confirmed the Chaser-side payload was one-sided by explicit design (`if ((side === "chaser") === isChaser)`), never reaching team clients at all — so `finalChaserQuestion` was simply never populated client-side for non-Chaser seats. Changed the guard to `side === "chaser" || !isChaser`: the Chaser-side payload (still only `{side, questionId, prompt}` — no answer field existed to leak in the first place) now reaches every seat, while the team-side payload stays exactly as one-sided as before (the Chaser must not get an advance look at the team's own final prompt — untouched). One existing server test (`server/test/finalRound.test.ts`) asserted the old one-sided behavior by name and had to flip — retitled and rewritten to assert the team now receives the identical `{side, questionId, prompt}` payload the Chaser does, plus a standing regression check that no `answer` field ever rides along. `ChaserFinalScreen.vue`'s template-only side: the non-Chaser branch now renders a read-only `.open-question-box` (no accent) showing `finalQuestion.prompt` plus a new `.oq-thinking`/`.oq-dots` pulsing "The Chaser is answering" indicator (added to 115's open-question-box family) instead of a blank status line; the Chaser's own `chaserAnswerInput`/`submitChaserAnswer` are unreachable from this branch (`isChaser` gates it) and were never part of any payload to begin with.

**New shared CSS this ticket introduces for 118 to reuse directly, not reinvent**: `.wrong-panel`/`.wrong-label`/`.wrong-answer` (dark card, red border, Luckiest-Guy "✗ WRONG!") — used here for both the Chaser's own wrong-answer/waiting-for-steal state and a resolved wrong steal; `.steal-success-panel`/`.steal-success-label`/`.steal-success-text` (same shape, green) for a successful steal; `.cf-hud`/`.cf-hud b` (VT323 HUD strip, gold bolded numbers) replacing the old prose "Time left: Ns · Chaser N — Target N" line — 118's Team Final should bind its own "Time left"/"Team score" line to this exact class rather than a `teamFinal`-prefixed one-off. Screen-specific (not meant for reuse): `.chaserFinalStage`/`.chaserFinalStage-steal`/`.chaserFinalStageVignette`/`.chaserFinalTensionFill` (+ `::after` edge glow) for the imposing red ground + full-bleed tension bar (a genuinely new layer behind the untouched `.finalTargetRow`/`.finalTargetBox`, not a replacement — confirmed the ticket's explicit "keep the target-box row as-is" instruction by leaving those two rules' declarations alone), and `.chaserFinalStealPromptTop`/`.chaserFinalStealLabel`/`.chaserFinalStealPrompt` for the steal-mode swap that replaces (not layers over) the Chaser banner, reusing the pre-existing `v-if="stealActive"` swap point in the template.

**Ground/vignette toggle**: `.chaserFinalStage` eases `background-color` from `--ink-chase` to `--color-bg-page` (the same plain navy Team-centric screens already sit on, per 115's own root-var comment anticipating exactly this) over 0.5s the instant `stealActive` flips, via one `chaserFinalStageClass` computed toggling a single `-steal` modifier; `.chaserFinalStageVignette`'s opacity fades to 0 in the same rule. This *replaces* the old full-screen `.chaseLockoutFlash` pulse the open steal window used to borrow (ticket 096) — with the ground itself now carrying the "team's moment" signal, `.chaseLockoutFlash`/`@keyframes chase-lockout-bg`/`--color-lockout-blue-rgb` had no callers left anywhere (grepped to confirm) and were deleted outright, closing the gap ticket 116's footnote explicitly deferred to this ticket. The resolved-outcome flash (`.chaserFinalStealFlash-correct/-wrong`, ticket 100's shared rule) is unchanged and still fires alongside the new success/wrong panels.

**`CharacterFace.vue` gained the `.head` wrapper** ticket 115's own footnote flagged as missing and ticket 116 explicitly declined to add (out of its own scope) — wired here because this is the first ticket that actually needs the shared head-wobble/eyes-squeeze reaction on a real character: a successful steal's winning seat gets it via a new `.teamFinalAvatarWrap.chaserFinalStealWinner` trigger class (extends the existing `.bust-pokeable.poked .head`/`.eyes` selectors rather than duplicating the keyframes — exactly the "differently-named trigger class" pattern that rule's own comment anticipated), driven by the already-tracked `stealAnswerSeatId`/`stealOutcomeCorrect === true`. Verified structurally safe before wiring it in: `.head`'s box is `position:absolute; inset:0`, which resolves to exactly the same box `.offerFaceWrap` already gave its layers, so every other `CharacterFace` call site (Lobby, Lineup, Cash Builder, Offer, Team Final, Chase) renders pixel-identical — confirmed by reading, not just asserting. The per-seat speech bubble ("pops a speech bubble over whoever's answer was accepted") needed **no changes at all**: ticket 096's `.chaserFinalStealBubble`/`.chaserPanelBubble` already renders the submitter's typed guess over the correct seat on every steal submission regardless of outcome, so this ticket's success case was already covered — it only adds the reaction class alongside the pre-existing bubble.

Refactored `stealOutcomeText` (one combined sentence) into `stealOutcomeLabel`/`stealOutcomeBody` (the correct/pushed-back-or-target-raised case) plus a separate `stealCorrectAnswer` ref (the wrong case, so the reveal can bold just the answer via `<strong>`, matching `TeamFinalScreen.vue`'s own existing "The answer was **X**" pattern instead of burying it in prose) — pure local component-state restructuring, same information, no message-shape or behavior change.

Real bug found and fixed in scope (not logged to `BUGS.md`, inside this ticket's own new code): the tension-fill (`position: absolute`, `z-index: auto`) painted *over* the target-row boxes instead of behind them — confirmed live, target box 1 visually disappeared under the fill the moment `chaserScore` reached it. Root cause is CSS2.1's stacking order, not a typo: a plain static (non-positioned) box always paints *behind* any `z-index: auto` positioned sibling regardless of DOM order, and `.finalTargetRow` had no `position` at all while `.chaserFinalTensionFill`/`.chaserFinalStageVignette` are both `position: absolute`. Fixed with a stacking-only `position: relative` added to the existing `.finalTargetRow` rule (no offsets, no layout change) — confirmed harmless on `TeamFinalScreen.vue`'s own unrelated use of the same class (no positioned siblings there to begin with). No other bugs found in scope; nothing added to `BUGS.md`.

`cd client && npm run build` (`vue-tsc -b && vite build`) and `cd server && npm test`/`npm run build` (308 tests, only `TriviaRoom.ts`/`finalRound.test.ts` touched server-side) all green. Live-verified with two real browser clients against the real dev servers (temporary, reverted-before-commit `client/src/App.vue` `client.create` overrides shortening every non-interactive phase — `chaserSelectionDurationMs`/`chaserRevealDurationMs`/`chaserCharacterRevealDurationMs`/`revealReadyCooldownMs`/`lineupDurationMs`/`cashBuilderDurationMs` — plus generous `teamFinalDurationMs`/`chaserFinalDurationMs` budgets to avoid racing the real clock mid-test, confirmed reverted via `git diff` showing zero residual trace before committing) through a full Chaser Final: the Watching state (the non-Chaser client showed the exact same `finalQuestion.prompt` plus the pulsing "The Chaser is answering" indicator; confirmed via direct DOM/`innerText` inspection that none of the Chaser's several typed wrong guesses ever appeared anywhere in the non-Chaser client's page — the one hard security requirement); the ground/vignette genuinely easing red→navy the instant a steal opened and back on resolve (observed both a claimed and an unclaimed-expiry close); a wrong steal (score/target unchanged, confirmed via server log); two successful steals at `chaserScore === 0` (target raised 1→2→3, tension-fill/target-row correctly inert since `chaserScore` itself never moved); and — the scenario the ticket most wanted confirmed — a successful steal at `chaserScore > 0` (2→1, the real "pushed back" case): tension-fill visibly shrank from ~67% to ~33% and target box 2 visibly un-filled while box 1 stayed filled, screenshotted both immediately before and immediately after. Did not manage to catch the ~3s success/wrong outcome panel itself mid-transition in a screenshot (a remote screenshot's round trip consistently lost the race against the resolve hold, even scripting both clients' actions in single batched calls) — its correctness is confirmed by code review plus the server-side outcome logs and the before/after state changes instead, not by eye.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 118: implemented unattended per the user's explicit direction to proceed with the already-agreed mockup direction (standing arrangement carried from tickets 115-117's footnotes) — built entirely from 115/117's shared primitives, no new bespoke components. `TeamFinalScreen.vue`'s HUD line now reuses `.cf-hud` verbatim, exactly as 117's own footnote flagged it would (`TIME LEFT`/`TEAM SCORE`, VT323, tabular-nums). The wrong-answer state swapped `teamFinalQuestionArea-wrong`/`teamFinalRevealLabel`/`teamFinalRevealAnswer` for a direct reuse of 117's `.wrong-panel`/`.wrong-label`/`.wrong-answer` (all three of those old classes deleted outright, not left as dead cruft). The buzz-in button became `.buzzer-btn` (115's raised-3D-press circle, built for this exact moment and unused until now) and the "someone's answering" watch state was rebuilt on `.open-question-box`/`.oq-eyebrow`/`.oq-thinking`/`.oq-dots` (mirroring 117's Chaser Final "Watching" state) instead of a bare `<p>`, for visual consistency with the rest of the final rounds — a presentation-only addition, no new state or behavior.

**The physical table**: new `.teamFinalTable`/`.teamFinalSeatsRow`/`.teamFinalTableSlab(-lit)` in `style.css`, matching the mockup's exact box-shadow/negative-margin values (`margin: -34px` on both the seats row and the slab, collapsing per CSS's own adjoining-sibling-margin rule to a single 34px overlap, not 68px — confirmed by first getting this backwards: an early pass put the player name *after* the avatar in DOM order, which put the *name* in the overlap zone and left the avatar floating with a visible gap above the table, defeating the "standing behind the table" read entirely; fixed by restoring the original name-then-avatar order, matching the pre-118 template exactly, which correctly puts the avatar's own shoulders in the overlap zone instead — caught and fixed via live screenshot inspection before commit, not left in the broken state). The team pot moved onto the slab's face in gold VT323 at 52px — deliberately a **new** class, `.teamFinalTableAmount`, not a reuse of `.teamFinalPotAmount`: `ResultsScreen.vue` (out of this ticket's scope) also renders a pot amount via `.teamFinalPotAmount` inside `.teamFinalPotBox`'s plain white card, and a first pass that changed `.teamFinalPotAmount` in place would have made that screen's black-on-white pot render as gold-on-white — caught before commit by grep'ing for every consumer of the classes this ticket touches, not just the ones the ticket names. `.teamFinalPotBox`/`.teamFinalPotAmount` themselves are therefore untouched and still exist, now documented in `style.css` as ResultsScreen-only. Buzzing does exactly the three things the ticket lists, all reused: (1) `.teamFinalPlayer.buzzing .head`/`.eyes` hooks into 115's generic wobble/squeeze keyframes (a new trigger class, same pattern as 117's `.chaserFinalStealWinner`); (2) `.teamFinalTableSlab-lit` (border-colour + glow, toggled off `finalBuzzSeatId !== ''`) lights the whole table, not a per-seat ring; (3) the existing `chaserPanelBubble`/`bubbleText` mechanism (already present pre-118) pops over the buzzer's own seat, now sized for the bigger avatar. The abandoned spotlight idea was not resurrected.

**Full-bleed target bars**: `.teamFinalTargetBg`, `position: fixed; inset: 0; z-index: -1`, genuinely fixed to the true viewport as the ticket explicitly demanded (not scoped to an inner wrapper the way the mockup's own demo page had to be) — a `<TransitionGroup name="team-final-target-bar">` renders one `flex: 1` bar per point of `teamScore` (a new `targetBars` computed, `Array.from({length: teamScore})`), so scoring narrows every existing bar and adds a new one that plays a `scaleY`-from-bottom rise-in via Vue's own enter transition — no leave transition needed since bars are only ever appended within a round. `chaserScore` is always 0 during this phase (confirmed against `ChaserFinalScreen.vue`'s identical `targetBoxes` comment), so unlike 117's target boxes these bars never "fill in" a colour, only accumulate — the old bounded `.finalTargetRow`/`.finalTargetBox` strip was dropped from this screen entirely (that shared class pair is untouched and still serves `ChaserFinalScreen.vue` alone now). The now-unused `chaserScore` prop was removed from `TeamFinalScreen.vue`'s `defineProps` and from `App.vue`'s binding to it, rather than left as a silently-ignored prop.

**Cross-screen sizing note, not spelled out in the ticket but confirmed deliberate**: `.teamFinalPlayer`/`.teamFinalAvatarWrap`/`.teamFinalPlayerName` are the exact same classes `ChaserFinalScreen.vue`'s Steal team row already reuses (ticket 117) — bumping the avatar from 64px to 110px and the name from 12px to 19px here therefore also grows the Steal row, which matches the design conversation's "bigger avatars" call across both final screens rather than fighting it; live-verified the Steal row still lays out correctly (frame-free, `flex-wrap`, no overflow) at the new size before committing, not just assumed safe from reading the CSS. `client build` (`vue-tsc -b && vite build`) and `server test`/`build` (308 tests, no server files touched) both green. Live-verified with two real browser clients against the real dev servers (temporary, reverted-before-commit `client/src/App.vue` `client.create` duration overrides — `cashBuilderDurationMs`/`chaserRevealDurationMs`/`chaserCharacterRevealDurationMs`/`revealReadyCooldownMs`/`lineupDurationMs` — confirmed reverted via `git diff` showing zero residual change before committing) through a full Lobby → Chaser Selection → Roles Reveal → Offer → Chase → Team Final → Chaser Final/Steal run: buzzing lit the table and wobbled the buzzer's head live, submitting a correct answer ticked `TEAM SCORE` and grew the full-bleed bars (visually confirmed the background split into two even tinted halves once the score reached 2, and confirmed via `document.querySelectorAll('.teamFinalTargetBar').length` matching the score exactly), the pot displayed correctly on the slab, and the subsequent Chaser Final/Steal round rendered the resized team row with no layout breakage. Did not manage to force a Team Final *wrong* answer within this same live run before the round's own clock advanced to the Chaser Final (the `.wrong-panel` reuse is byte-identical to 117's already-live-verified version, so this is a low-risk gap, not left silently unconfirmed) — noted here rather than glossed over. No bugs found in scope; nothing added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 119: implemented unattended per the user's explicit direction to proceed with the already-agreed mockup direction (standing arrangement carried from tickets 115-118's footnotes) — almost entirely reuse-and-restyle, per the ticket's own framing, with one real bug caught and fixed along the way. The room code became a real first use of 115's `.ticket-badge` (built in that foundation ticket but never actually wired into a screen until now) — `.roomCode`'s old plain-pill markup/CSS were deleted outright, not left alongside the new badge. The table ring itself (`.lobbyCircle`, 380px -> 440px) now IS the physical table (raised 3D block, same lip-shadow trick as 118's table slab/115's buttons) with 115's `.lobby-start-btn`/`.lobby-play-triangle` (built for this exact moment, unused until now) floating centered on top for the host — the old separate `.lobbyTable` visual disc and `.lobbyStartButton`/`.lobbyStartIcon` button were deleted, not restyled in place, since the ring's own background now serves that role. `SEAT_RADIUS` bumped 130 -> 150 alongside the bigger ring (the position *formula* in `seatPositions` is untouched, only this one distance) so seat content sits with real clearance inside the ring's silhouette, per the ticket's explicit "build with generous clearance from the start" instruction — confirmed this was a deliberate presentational tuning knob, not the "seat-position math" the ticket's own "should not change" list was protecting (that list groups it with `poke()`'s state model, i.e. the *algorithm*, not this *constant*; freezing the literal old radius would have fought the very redesign the ticket asked for, since the ring grew but the seats would have stayed clustered near its center).

**Real bug found and fixed during implementation** (self-introduced this same session, caught before commit via live browser verification, not shipped): the new `.lobbySeatCircle`/`.lobbyCharacterPreview` size-override rules were first written as bare single-class selectors, which lose the CSS cascade to `.board-portrait-circle`'s own later-declared `width:100%/height:100%` (equal specificity, later source order wins) — confirmed live via `getBoundingClientRect()`, every seated player's circle rendered with the correct width but a collapsed **zero height** (a percentage height against `.lobbySeat`'s auto-height flex-column parent, with no non-absolutely-positioned children to establish one, resolves to 0), silently hiding every face in the lobby. Fixed by compounding both selectors (`.board-portrait-circle.lobbySeatCircle`, `.board-portrait-circle.lobbyCharacterPreview`) — the same higher-specificity trick `.board-portrait-wrap.chaser-wrap` already used in 115/116 for exactly this reason, just not one this session initially recognized needed repeating here. Documented inline in `style.css` so the next screen that reuses `.board-portrait-circle` doesn't rediscover this the hard way.

Seated players are frame-free circles (`.board-portrait-circle`, sized via the new compound rule to 84px) with no bordered/tinted box behind them — the old `.lobbyBust`/`.lobbyBustInner`/`.lobbyBustSvg`/`.lobbyBustAvatar` classes and their per-seat `nth-child` accent-colour rules were deleted wholesale (not restyled), along with the now-fully-orphaned `--avatar-coral/-teal/-amber/-pink/-purple` tokens those rules were their only consumers of (confirmed via repo-wide grep before deleting). Empty seats reuse the dashed-outline idea via a new `.lobbyEmptyCircle` at the same 84px size, restyled from the old bordered-box placeholder rather than kept alongside it. Poking replaces the old whole-bust `.lobbyBustInner.poked`/`lobby-bust-poke` squash entirely with the shared `.head`/`.eyes` wobble/squeeze reaction (115) via a new `.lobbySeatCircle.poked` trigger — confirmed removed via grep (`lobby-bust-poke`/`lobbyBustInner` have zero remaining matches outside one explanatory code comment) per the ticket's own explicit acceptance check. The pre-existing pop-in-on-join entrance animation (`lobby-bust-pop`, a different animation from the poke squash and not called out for removal) was preserved and retargeted onto the new circle. `poke()`/`pokedSeatId`'s state model, `seatPositions`' angle formula, `saveCharacter()`/character-picker state, room-code copy logic, and `setChaserMode` are all untouched — only presentation, per the ticket's own scope line.

The customizer preview became a frame-free circle (`.board-portrait-circle.lobbyCharacterPreview`, 100px) instead of a bordered rounded-square box; all five rows (not just the mockup's one demoed hair-colour row) got the chunky-circle-swatch treatment — `.lobbyCharacterSwatchButton` (hairstyle/face number pickers) restyled as chunky circular ink-bordered buttons matching `.lobbyCharacterColourSwatch`'s existing circular shape, both bumped in size; the Save button became `.btn.btn-primary` (115) with its own restyled `:disabled` state rather than the old plain `.startButton`. The settings dropdown's two mode buttons got the chunky flat-press border/shadow treatment per the ticket's explicit ask, deliberately kept in light/white-appropriate colours since the dropdown card itself is out of this ticket's scope (not specifically redesigned) and untouched.

`cd client && npm run build` (`vue-tsc -b && vite build`) and `cd server && npm test`/`npm run build` (308 tests, no server files touched) all green. Live-verified with two real browser clients against the real dev servers through Lobby end to end: both seats rendered real faces at the corrected size (confirmed via `getBoundingClientRect` after the fix, not just visually), poking a seat's `.head`/`.eyes` wobble/squeeze wiring confirmed correct via direct class/computed-style inspection (`animationName: "head-wobble"` on toggling `.poked`) after discovering the automated click path itself is unreliable to verify in this environment — `poke()`'s own `requestAnimationFrame` scheduling silently never fires while the Browser pane is backgrounded/hidden, a tooling limitation of this session, not a code defect (the click/state-binding code itself is untouched, pre-existing, and already shipped correctly before this ticket); changing a hair-colour swatch correctly dirtied and chunky-blued the Save button, saving it live-updated the host's own seated avatar; the Settings dropdown opened with correctly-styled mode buttons; and clicking the host's `.lobby-start-btn` correctly advanced the room into Chaser Selection with no console errors on either client. Room-code copy-to-clipboard itself could not be confirmed end-to-end (the automated browser's clipboard permission silently no-ops, a pre-existing/unchanged code path per the ticket's own "stays exactly as it is today" instruction, not something this ticket touched). No new bugs found in scope beyond the one caught and fixed above; nothing added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 120: fixed per the ticket's primary suggestion — dropped the separate `lineupFirstTag` ("Up first") span entirely rather than the alternative (suppressing the ordinal for position 1), since `.lineupCard-first`'s existing pulsing highlight animation plus the "1st" badge already read as a clear enough "you're up" signal once live-verified on screen, with no need for a second overlapping label. Deleted the now-fully-orphaned `.lineupFirstTag` CSS rule outright (confirmed via grep it had no other consumer) rather than leaving it dead. No other markup/behavior changed — `ordinal()`, `lineup` computed, and every other position's rendering are untouched. `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests, no server files touched) both green. Live-verified with two real browser clients through Lobby → Chaser Selection → Roles Reveal → Lineup: the single contestant's card showed exactly one "1st" badge with no duplicate tag. No bugs found in scope; nothing added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 121: implemented per the ticket's proposed direction — confirmed first (per the ticket's own instruction) that `submitAnswer` in `server/src/rooms/handlers/messageHandlers.ts` already sends `correctAnswer` on both outcomes via `client.send("answerResult", {correct, correctAnswer, questionId})`, so no server change was needed. Added a new `correctAnswerText` ref (not reusing `revealedCorrectAnswer`, which has a different lifetime) rendered via a new `.cashBuilderQuestionArea-correct`/`.cashBuilderRevealLabel-correct` pair — the wrong case's exact card treatment, re-accented green instead of red, reusing `.cashBuilderRevealLabel`/`.cashBuilderRevealAnswer` as-is.

**Real bug caught and fixed during implementation, not shipped**: the first pass cleared `correctAnswerText` in the `currentQuestion` watch (mirroring how `revealedCorrectAnswer` is cleared there) — confirmed live via `getComputedStyle`/DOM polling that this made the confirmation visible for effectively 0ms, because `submitAnswer` calls `advanceQuestion()` **synchronously** on a correct answer (unlike a wrong answer, which goes through `scheduleTimer(wrongAnswerRevealMs, advanceQuestion)` — a real delay), so the next `currentQuestion` prop routinely arrives within the same tick as the correct-answer watch itself, wiping the text before it could ever paint. This is the exact same class of bug ticket 107 already fixed once for `bubbleText` (documented in this file's own code comments) — I initially missed that the new ref needed the identical treatment. Fixed by leaving `correctAnswerText` out of the `currentQuestion` watch entirely and letting only its own 700ms timeout (matching the correct flash's existing cadence, per the ticket's own suggested option) clear it — confirmed via a poll loop that catches the reveal at t=0ms after submission now, versus never catching it before the fix.

Judgment call: kept the reveal scoped to the active contestant only (same visibility as the existing wrong-case reveal, which is likewise `v-if="isActiveContestant"`-gated) rather than also broadcasting to spectators, even though the acceptance text mentions "own client and spectators" — `answerResult` is sent via `client.send` (submitter-only) today for both outcomes, so spectators already can't see either reveal; extending that would need a new server broadcast, which the ticket explicitly rules out ("Don't change any server messages"). Followed the stronger, more specific instruction over the softer acceptance wording. `cd client && npm run build` and `cd server && npm test`/`npm run build` (308 tests, no server files touched, confirmed no server change was needed rather than assumed) both green. Live-verified with two real browser clients against the real dev servers: a correct Cash Builder answer now shows a green "✓ Correct!" card with the accepted answer text for a readable beat before the next question replaces it (confirmed both visually and via DOM inspection immediately after submission), a wrong answer still shows the pre-existing unchanged "✗ WRONG!" reveal. No bugs found outside the one caught and fixed above; nothing added to `BUGS.md` for this ticket.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* 122: implemented per the ticket's proposed direction, mirroring ticket 049's already-proven Lineup pattern exactly — a new `GamePhase.TeamFinalIntro` sits between the last Chase resolving and the real Team Final starting. `server/src/TriviaTypes.ts`/`client/src/TriviaTypes.ts` both got the new enum value (`teamFinalIntro`), confirmed byte-identical via `diff` per the existing convention (not a manual grep, matching AGENTS.md's own note on how this parity is normally verified — build/test success across both packages is the real check, the diff was just an extra sanity pass). `gameFlow.ts`'s `chaseEscape`/`chaseCaught`/`contestantForfeit` branches that used to push `startFinalTeam` + jump straight to `GamePhase.TeamFinal` when there's no next contestant now push a new `startTeamFinalIntro` effect and land on `GamePhase.TeamFinalIntro` instead; a new `teamFinalIntroComplete` event (dispatched by the room's own timer, mirroring `lineupComplete`) carries the flow the rest of the way into the *real* `startFinalTeam` effect + `GamePhase.TeamFinal` — the same two-step shape `Lineup`→`lineupComplete`→`CashBuilder` already uses. Added `GamePhase.TeamFinalIntro` to `chaserForfeit`'s `validPhases` list (both the real one in `gameFlow.ts` and its mirror in `gameFlow.test.ts`) alongside the already-present `Lineup`, for the same reason: a Chaser forfeit during this brief hold should still resolve to game-over rather than being rejected. New `TEAM_FINAL_INTRO` config block in `gameConfig.ts` (`durationMs: 5_000`, clamped like every other duration config) and a `teamFinalIntroDurationMs` field on `TriviaRoom` wired through `clampOptions.ts`, matching `lineupDurationMs`'s exact pattern throughout.

**Real bug caught and fixed during implementation, not shipped**: the client-side wiring was done in three parts — importing `TeamFinalIntroScreen.vue`, mapping `GamePhase.TeamFinalIntro` to the `"teamFinalIntro"` screen-name string in `currentScreen`'s computed switch, and adding the actual `<TeamFinalIntroScreen v-if="currentScreen=='teamFinalIntro'">` element to the template — the first pass did the first two and silently forgot the third, so the phase transitioned correctly server-side (confirmed by the dedicated new gameFlow tests and by the "Phase -> teamFinalIntro" log line appearing during a live run) but nothing ever rendered — the screen just silently skipped from Chase straight to Team Final on screen, the exact bug this ticket exists to fix, reintroduced by its own incomplete wiring. Caught via a live browser walkthrough with a `window.setInterval` DOM poll logging every `h2` change with a timestamp (a plain `get_page_text`/screenshot round trip couldn't reliably catch a multi-second transient window against several-second tool round-trip latency) — the poll showed the screen jumping straight from "The Chase" to "The Team Final" with no interstitial title in between. Fixed by adding the missing template element; re-verified with the same poll technique at two different `teamFinalIntroDurationMs` overrides (4000ms and 8000ms) and confirmed the "Onto the Team Final!" title appeared and held for almost exactly `configured duration − 2500ms` each time (2500ms being `CHASE_RESULT_HOLD_MS`, the pre-existing client-side CAUGHT!/ESCAPED! cutscene hold in `App.vue` that buffers incoming phase broadcasts via `pendingPhase` while a cutscene is playing — worth knowing for whoever next touches a phase boundary near a chase outcome: a hold shorter than the cutscene's own hold gets fully swallowed and never shown at all).

The new `TeamFinalIntroScreen.vue` mirrors `ContestantLineupScreen.vue`'s pattern closely, as instructed: no interactivity, no local timers, just a plain list auto-driven by the server's phase timer. Shows only the survivors (`contestant.madeItBack`), reusing the exact `lineupScreen`/`lineupTitle`/`lineupList`/`lineupCard`/`lineupAvatar`/`contestantName`/`contestantYou` classes already proven out by ticket 120's Lineup fix earlier this same session — no new CSS at all, per the ticket's explicit "don't invent new chrome here" instruction, deferring the *look* of it to whichever future redesign round reaches this screen. Reused `App.vue`'s existing `lineupContestants` computed (already the exact "ordered contestants, chaser excluded" list Lineup itself uses) as this screen's own data source rather than deriving a second one.

Scoped to the Chase→Team-Final boundary specifically, as instructed — did not touch the Team Final→Chaser Final or Offer→Chase boundaries. Noting for a future ticket, per the ticket's own request: once this landed and was played through live, the Team Final→Chaser Final cut still feels exactly as abrupt as the Chase→Team-Final one used to — the same `TeamFinalIntro`-style holding-phase pattern would likely read well there too, but that's a new ticket's call, not an expansion of this one's scope.

`cd server && npm test` (310 tests — 2 new: `teamFinalIntroComplete -> finalTeam` and its guard, plus updated assertions in three existing describe blocks for the new intermediate phase) and `cd server && npm run build` both green; `cd client && npm run build` green (`vue-tsc -b && vite build`, 80 modules). Six server test files needed `teamFinalIntroDurationMs` room-option overrides added (mechanical — every room that walks a real Chase resolution to Team Final/Chaser Final needed the same short override already given to `lineupDurationMs`, or the real 5-second default would have stalled them): `chaseFlow.test.ts`, `chaserPot.test.ts`, `finalRound.test.ts`, `leaveFlow.test.ts`, `roomFlow.test.ts`, plus `clampRoomOptions.test.ts` for direct clamp coverage of the new field. Two direct `assert.strictEqual(room.state.currentPhase, GamePhase.TeamFinal)` checks in `chaseFlow.test.ts` (asserted immediately after a `while (currentPhase === Chase)` polling loop exited) were racy against the new intermediate phase — the phase would genuinely be `TeamFinalIntro`, not yet `TeamFinal`, at that exact instant — replaced with the file's own existing `waitForPhase` polling helper rather than a raw equality check. Live-verified with two real browser clients against the real dev servers through a full Lobby → Chaser Selection → Roles Reveal → Cash Builder → Offer → Chase (caught) → **Team Final Intro** → Team Final run, confirmed both via the DOM-poll technique above and by screenshot once the timing was well understood. No bugs found outside the one caught and fixed above; nothing added to `BUGS.md` for this ticket.

Status values: `backlog`, `in-progress`, `done`.

Conventions in play (see `AGENTS.md`): server tests live in `server/test/*.test.ts` (picked up by `npm test`); no lint config — verify with `npm run build` on each package.